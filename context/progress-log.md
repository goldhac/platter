# Platter — Progress Log

> The living record of what has actually been built and decided. Newest entries first (prepend).

## ⚠️ Standing instruction for the AI agent

**After completing any work in this project, before ending your response, add a progress entry below.** This is mandatory — the same way reading the context files first is mandatory (`code-standards.md §1`).

**The drift rule:** if your entry is a `decision` that changes anything in `foundation.md` or another context file, **update that file too** and add a companion `docs` entry noting it. Context files must never drift from what was actually decided (`README.md` golden rule).

### Entry template

```
### YYYY-MM-DD · <category> · <short title>
- **area:** backend | apps/public | apps/admin | db | infra | context | lib/<x>
- **what:** one line (itemize parts if one prompt produced several)
- **notes:** gotchas, limitations, follow-ups
```

`category` ∈ `feature` · `fix` · `refactor` · `chore` · `decision` · `docs`

---

## Entries

### 2026-08-13 · feature · Landing polish — on-brand photos, hero depth, motion, premium themes
- **area:** components/marketing, public/images/marketing, app/globals.css
- **what:** Rounds of polish on the product-led landing, per Gold's direction. (1) **On-brand photography:** replaced the generic Unsplash stand-ins with 5 generated stills (MuAPI `seedream-v4`, $0.20 total) matching the Jīn Cāntīng world — hands photographing a paper menu, a laptop mid-edit, a QR table-tent on a set table, a red-lacquer restaurant interior, an overhead dish spread; downscaled + compressed into `public/images/marketing/`. (2) **Hero depth/layering:** two blurred glow orbs (oxblood + gilt), a ghost panel stacked behind the browser frame, a floating "Price updated · live" toast plate, and gentle ambient float on the frame + phone. (3) **Animation across the board:** a `reveal-observer.tsx` client island drives `data-reveal` scroll-fade-ups on 25 blocks (visible-by-default so no-JS/crawlers are fine), insights bars grow left-to-right on reveal, hero layers breathe — all gated by `prefers-reduced-motion`. (4) **Premium theme cards** (Gold flagged the mono-caps labels as not-premium): now editorial menus — venue name + `Nº 0X` index + accent underline rule, an italic **serif** theme name (Lacquer/Carafe/Counter/Palm) instead of mono caps, and a faint `餐` watermark. (5) **Section watermarks:** oversized faint `金`/`色`/`价` glyphs behind Features/Themes/Pricing for depth.
- **notes:** typecheck + token-lint + build green. Verified via live-DOM query (screenshot capture was glitchy on lower sections this session): reveal system armed + firing, all premium theme elements present (index, serif names, 餐 watermark), floating chip + 2 float frames live. Hero-with-depth captured cleanly and reads premium. Photos are AI-generated stand-ins that match the brand; still worth swapping for real Jīn Cāntīng shots eventually. Reveal hover-transition on `lp-step`/`lp-price-row` is superseded by the reveal transition (imperceptible).

### 2026-08-13 · feature · v2 REDESIGN — marketing landing rebuilt product-led (handoff v2)
- **area:** components/marketing, app/globals.css
- **what:** Replaced the restaurant-photo-led landing with the product-led design from the new `LANDING.md` handoff. Nine sections in order: **hero** (the product IS the hero, a markup-built browser frame showing the menu editor + the diner's phone overlapping it, both from the real tokens; live-venue struck plate; "Replaces" strike-through strip), **Set up in one evening** (3 step cards), **Features** (2 headline cards — AI import with flag list + Dynamic QR with an engraved gilt tent SVG — then 6 seal cards 印厨素价众问), **mid-service sold-out** (full-bleed scrim + a phone mock with toggles), **Themes** (4 live previews reading each theme's real palette from `lib/themes/*` so it can't drift — Counter renders light, Palm green), **Insights** ("searched for, not found" bars), **Pricing** (10-row Free/Pro ledger + ₦0/₦12,000), **FAQ** (`landing-faq.tsx` client island, one-open accordion), **CTA** (food, at last). New struck-**plate** component (appears 3×). Copy rules honored: **no eyebrows, zero em dashes** (grep-verified). Hero entrance + card/row hovers added as `.lp-*` classes in globals.css, all gated by `prefers-reduced-motion`.
- **notes:** typecheck + token-lint + build all green. Verified: hero + step cards render faithfully on screen; live-DOM query confirms 6 seal cards, 4 theme previews with the right manifest backgrounds (Lacquer #0a0807 · Carafe #0d0d0f · Counter #faf8f4 light · Palm #123a2e), 10 pricing rows, 6 FAQ items. (mcp-browser screenshot capture glitched on the lower sections — a capture quirk, not a page fault; DOM is authoritative.) Photos are free-licence Unsplash stand-ins (food only in the closing CTA) — swap for real Jīn Cāntīng shots before launch, same scrim values. The old `/pricing` + `/themes/[id]` remain transitional.

### 2026-08-13 · chore · Published to GitHub (public) + first commit of the full tree
- **area:** infra
- **what:** Created **github.com/goldhac/platter** (public, default `main`) and pushed the whole project as one commit (153 files) — the first time the codebase is off-machine. Added a public-facing `README.md` (what Platter is, live link, feature table, stack, structure, local-dev, deploy; the old context-system map is folded into a "Working on this repo" section so CLAUDE.md's contract holds) and completed `.env.example` (all env vars the app reads, placeholders only). Fast-forwarded local `main` (was 21 behind) to the work and switched onto it.
- **notes:** **Secret audit before pushing:** `.env.local` is git-ignored and confirmed absent from the remote; a tree-wide scan for JWT/`AIza`/`re_`/`sk-`/`gh*_` patterns found nothing hardcoded. Deploys still go via `railway up` (Railway isn't wired to GitHub) — git is now the backup/history, not the deploy trigger. **Heads-up (flagged to Gold):** `context/` + `docs/` are now public, incl. `foundation.md` (business/pricing reasoning), the PRDs, and the roadmap — prune/gitignore those if internal-only was intended.

### 2026-08-13 · feature · v2 REDESIGN — item detail sheet (menu now coherent, ready to ship)
- **area:** components/menu
- **what:** Rebuilt `item-sheet.tsx` to the v2 ceremonial language — the last diner-menu piece. Gilt grabber; **hero** = photo in a `rounded-lg` framed skeleton, or (no photo) the debossed **seal plate** with a large `framed` `Seal` (glyph = the dish's own 中文 initial, else 餐); **Bodoni** title + gilt (`hairline-strong`) 中文 subtitle; chef/spicy/veg `SealMark`s top-right; a **L→R gilt-fade rule**; the **full** description (the reveal the one-line row withholds); price as a **Martian-mono ledger figure** with the `≈ $` dual-currency line (portion cards when variants exist, each with its own secondary line); **octagon `SpicePips`** + spice label; allergen/dietary chips; a letterspaced mono CLOSE. Kept the Radix Dialog a11y (role/aria-modal/focus-trap/Esc/restore) and the `data-sheet` hooks that drive the slide-up/fade in globals.css.
- **notes:** typecheck + tokens + build all green; **verified in-browser** — sheet opens over a dimmed overlay, slides up, renders the photo hero + Bodoni title + mono ₦9,000 / ≈ $5.85 + CLOSE, zero console errors. **Diagnosed Gold's "error when I click each one":** a `ChunkLoadError` for a dead lazy chunk (`0830…js`) — **not a code bug.** The live site (v1 sheet) opened fine; the local `next start` was serving an in-memory manifest from a *previous* build after I rebuilt on top of it, so the browser fetched a chunk hash no longer on disk. Fix = deterministic: kill server → `rm -rf .next` → `next build` → `next start`; a fresh document then loads the new chunks. (mcp-browser quirk noted: raw-coordinate clicks don't trigger MenuBoard's delegated handler; ref-clicks / real taps do.) **DEPLOYED to production** (`railway up` → platter.goldhac.com, build clean, no new migration — menu redesign is pure frontend). Verified live: HTML carries the "Most ordered" header + octagon-seal clip-path; opening a dish shows the v2 sheet (Bodoni title, mono ₦9,000 ≈ $5.85, CLOSE) with **zero console errors**. The whole v2 diner menu (header · rows · seals · sections · sheet) is now live and coherent. NB: the entire working tree ships uncommitted via `railway up` (project's established pattern) — no git remote; worth a commit to capture this state.

### 2026-08-07 · feature · v2 REDESIGN — diner menu (screen built + verified, not yet shipped)
- **area:** lib/themes, components/menu
- **what:** Redesigned the diner menu to v2. **Theme engine:** `lib/themes/lacquer.ts` → v2 tokens (dark #0a0807/#151110 surfaces, oxblood #8d2020, 10%-bone hairline, sage positive) + v2 faces (Bodoni/Public Sans/Martian); the extra v2 roles (surface-raised, hairline-strong/gilt, text-tertiary, accent-hover) fall through to the @theme Lacquer defaults, so the menu gets a consistent palette. **Components:** rebuilt the seal as a **chamfered octagon** (`Seal`/`SealMark`/`SpicePips`, token-based), the **ceremonial header** (framed 金 seal + share/print icon buttons, 中文 gilt, Bodoni venue name, rule-flanked location, open/closed pill), the **item row** (name + 中文 + chef seal · description · price/≈/veg/spice meta row · 74px photo-or-seal thumb), the **thumb** (octagon-seal fallback), and menu-screen's **section headers** (Bodoni 31px + 中文 + L→R gilt-fade rule), **"Most ordered"** shelf header, and subtle group eyebrows. The sticky bar (search/chips/rail) re-skinned to v2 automatically via the tokens.
- **notes:** typecheck + tokens + build green; **verified rendering in-browser + via SSR markers, no console errors** — the ceremonial header + oxblood/gilt/bone + Bodoni + octagon seals all land (Chrome-CLI screenshot flaked on this route only, an env quirk). **The live menu is unchanged — NOT deployed** (last deploy = v2 marketing + v1 menu); holding the menu deploy until the **item-sheet** overlay is rebuilt to v2 (the one remaining piece) so it ships coherent. Other themes (carafe/counter/palm) still carry v1 base tokens + v2 fallbacks — fine (flagship is Lacquer); reconcile later.

### 2026-08-07 · feature · v2 REDESIGN — Themes page + marketing set shipped
- **area:** app/themes, infra
- **what:** Rebuilt `/themes` to the v2 design — hero ("A menu that looks like *you.*") + jump chips, then all four themes each in a full section (index/scheme/Free-Pro badges, tagline, reason, best-for chips, spec grid Display/Layout/Photos, accent swatches, CTAs) with a **live preview rendered in that theme's own tokens + layout** (list-dense/ruled-list/card-grid/editorial), + a "Switching is one tap" strip + CTA. Theme token values mirror `lib/themes/*`. Counter renders as the light scheme correctly; Carafe shows leader-dots + no photos; Lacquer shows the 餐 seal for a no-photo dish. **The v2 marketing set is complete + verified** (Landing · Discover · Themes · shared shell) and deployed.
- **notes:** typecheck + tokens + queries + build green; verified via headless — the four previews read genuinely differently, per the handoff's intent. The old `/themes/[id]` detail route is now orphaned (unlinked; still valid) — fold into a later pass or remove. **Next:** Auth & Onboarding, then the diner menu (update lib/themes to v2 faces + rebuild), then the admin.

### 2026-08-07 · feature · v2 REDESIGN — shared marketing shell + Discover (verified)
- **area:** components/marketing, app/discover
- **what:** v2 marketing chrome: rebuilt `MarketingShell` (sticky blur nav bone-mark + Discover/Themes/Pricing/Sign in + Get started, and footer; `active` prop). Rebuilt `/discover` to the v2 design — gilt-eyebrow hero ("Eat somewhere *good.*"), a client `DiscoverGrid` (search + cuisine chips derived from real venues, filtering; chamfered-octagon **seal** fallback for no-photo; 空 empty state), the real Jīn Cāntīng card (photo, Live pill, 金餐厅, cuisine, blurb, View menu →), and the "Put yours *here.*" CTA. Omitted the prototype's dev-facing "honest-state" callouts (getListedVenues field notes) — those were notes to me, not diners. Open-now / dish-count deferred (need hours-join + count-join per the handoff).
- **notes:** typecheck + tokens + build green; verified via headless render — faithful. **Marketing set remaining: Themes** (the complex one — 4 per-theme previews in each theme's own tokens/layout, data-driven from lib/themes), then deploy the marketing set as one piece.

### 2026-08-07 · feature · v2 REDESIGN — token foundation + marketing homepage (verified)
- **area:** app (layout/globals), components/marketing, public/brand
- **what:** Implemented the Claude Design handoff (`design_handoff_platter_v2` — a 23-screen full redesign). **Foundation:** new faces via next/font — **Bodoni Moda** (display) · **Public Sans** (text) · **Martian Mono** (ledger) · kept Noto Serif SC; rebuilt the `@theme` token block in `globals.css` to the v2 semantic contract (dark-first oxblood/gilt/bone, #0a0807 bg, +7 new roles: surface-raised/sunken, text-tertiary, hairline-strong (brass→ceremonial), danger, caution, accent-text, + the accent trio; radius scale; e1–e3 elevation; motion durations/eases). The **diner menu is insulated** — it reads fonts/colours via `lib/themes` (ThemeProvider), so v1 faces stay loaded and the live menu is unchanged; @theme drives marketing + admin. **Homepage:** rebuilt `MarketingHome` to the cinematic v2 landing — full-bleed hero, Bodoni "Make them *hungry.*", floating live-menu card (chamfered-octagon seal spice-pips, dual-currency, sold-out), marquee, dish bento, AI-import mockup, ledger pricing, `<details>` FAQ, full-bleed CTA, footer. Copied the mono logo lockups (bone/ink/lacquer) to `public/brand/`.
- **notes:** typecheck + tokens + build green; verified via headless render — faithful. Removed a dead orphan `components/admin/qr-tools.tsx` (unused, superseded by `qr-studio.tsx`, stale 2-arg `generateTableTentPdf` call) that was breaking tsc. **Two brand calls in the handoff to confirm with Gold:** (1) fonts changed from Fraunces/Inter/Plex; (2) the logo went **mono — the orange was dropped** (handoff: orange clashes with the oxblood accent). Admin + secondary marketing pages are transitional (v1 layout + v2 tokens) until rebuilt. **Next:** Marketing Themes + Discover + shared shell, then deploy the marketing set; then auth → menu (update lib/themes) → admin.

### 2026-08-07 · docs · Design-system prep — light logo + tokens.md + Claude Design prompt
- **area:** infra (public/), context/docs
- **what:** Generated a **light logo variant** (`public/logo-light.png` + `logo-mark-light.png`) — recolored the dark mark to porcelain, kept the orange, transparent bg (PIL/numpy); verified it reads on the dark chrome. Wrote `docs/tokens.md` (real current token values from `@theme` — framed as a starting point) and `docs/CLAUDE-DESIGN-PROMPT.md` (a loose creative brief telling Claude Design to build the design system foundations + primitives with freedom to evolve, keeping only the token role names). Rebundled `platter-redesign-brief.zip` (5 md + 31 images incl. the light logos).
- **notes:** Workflow: Gold builds the design system in Claude Design from the prompt → I implement it as a `components/ui/` primitives layer on the token contract → then screens (marketing first). No app-code changes.

### 2026-08-07 · docs · Redesign reorder → marketing-first + BRAND.md
- **area:** context/docs
- **what:** Reordered the redesign to start with the **marketing site** (homepage/pricing/discover/themes) before the public menu — it's independent of the tenant token system (own chrome), pure-presentational, and where the Platter brand/logo gets settled. Added `docs/BRAND.md` (platform brand reference: positioning, voice, palette hexes, type, logo gap) for Claude Design; updated `REDESIGN-PLAN.md §0.4`; rebundled the handoff zip. Skipped `/design-sync` — repo is a Next app, not a component library (user chose the brief-based handoff).
- **notes:** Open asset gap: the logo is dark-on-white and can't sit on the dark marketing chrome — needs a light/mono variant (offered to generate). No app-code changes.

### 2026-08-07 · docs · Redesign plan (design-system-first, phased)
- **area:** context/docs
- **what:** `docs/REDESIGN-PLAN.md` — phased plan: P0 lock direction → **P1 build the design system** (tokens → primitives → brand) → P2 design public menu → P3 implement (remap the token layer + rebuild components) → P4 marketing/auth → P5 admin → P6 motion/polish/a11y. Anchored on the token-driven architecture so the redesign is a token remap + component rebuild, not a rewrite. Names the design→code loop + the immediate next step.
- **notes:** Lives in the repo so it uploads with the codebase. No code changes.

### 2026-08-07 · docs · Redesign brief + screenshot set for Claude Design
- **area:** context/docs
- **what:** `docs/REDESIGN-BRIEF.md` — a full inventory of every screen, modal, drawer, component, and state (public menu · marketing · auth · admin), the brand/token system, the 4 themes, and the redesign guardrails (semantic tokens, 215KB budget). **28 real screenshots** in `docs/redesign-shots/` via headless Chrome: public menu + item-sheet modal + the menu in all 4 themes + print + 5 marketing + auth, **and all 14 admin screens** (captured through Gold's logged-in Chrome session via a temp-profile cookie copy — never typed a password). Bundled as `docs/platter-redesign-brief.zip` (+ `REDESIGN-PLAN.md`) for handoff.
- **notes:** Admin capture gotcha: `--headless=new` + a real profile never self-exits, and macOS has no `timeout`/`gtimeout` — used a Python subprocess-timeout wrapper to kill Chrome once each screenshot was written. Temp profiles/cookies cleaned up after. Redesign scope confirmed = public menu first. No app-code changes.

### 2026-08-07 · fix · Image skeleton (kill the green flash) + multi-page print
- **area:** apps/public
- **what:** **(green flash)** The image `blurDataURL` was a **pure-green 1×1 pixel** `(0,255,0)` mislabelled "neutral dark" — next/image blur-upscaled it into a green flash before photos loaded. Removed the blur placeholder entirely (+ the bad export) and added a theme-aware **`.img-skeleton`** shimmer (globals.css, `prefers-reduced-motion` respected) on the thumb + item-sheet-hero containers; the loaded object-cover image paints over it. **(print pagination)** Print menu now paginates onto **A4** (`@page { size: A4 }`) — categories flow across pages, a dish `<li>` never splits (`break-inside: avoid`), a category heading never orphans (`break-after: avoid`); removed the section-level inline `break-inside: avoid` that would have trapped long categories on one page.
- **notes:** typecheck + tokens + build green; deployed. Skeleton is CSS-only (no per-thumb hydration, keeps the server-rendered rows lean).

### 2026-08-07 · feature · Backlog batch — concierge · share · print · no-results · dual-currency
- **area:** apps/public, apps/admin, lib
- **what:** Five backlog features. **(LLM concierge)** `/api/concierge` grounds **Gemini 2.5 Flash** on the venue's live menu (built server-side from getMenu) → a floating chat widget (`components/menu/concierge.tsx`, dynamic-imported, diners only). Verified live — it recommends real dishes with correct prices. **(social share)** `MenuActions` in the header — Web Share API with copy-link fallback (local feedback; the public menu has no Toaster). **(in-theme print)** `/v/<slug>/print` — a paper-optimised menu in the venue's theme fonts + accent, `@media print` CSS, "Save as PDF" button. **(no-results "Add it?")** an empty search now offers **"Ask us on WhatsApp"** (wa.me deep link prefilled with the query, `lib/share.ts`) when the venue has a WhatsApp number. **(dual-currency)** an optional second currency (code + rate) in Settings → an "≈ $X" line under prices in the item sheet; config lives in `restaurants.theme` jsonb (no migration), threaded through getMenu → MoneyOpts → `formatSecondary`.
- **notes:** typecheck + tokens + queries + prod build green; deployed. **Gemini gotcha:** `gemini-2.0-flash` is retired for generateContent (404); `gemini-2.5-flash` burns the whole output budget on "thinking" unless `thinkingConfig.thinkingBudget = 0` — set that + `gemini-2.5-flash`, verified. Concierge/print are code-split off the menu's first-load. Set a demo NGN→USD rate (0.00065) on the flagship to show dual-currency. **Not built (need external things):** full margin-based menu-engineering (needs per-item cost + ideally real sales data — Popular/Insights is the shipped v1); WhatsApp *sold-out toggle* (needs WhatsApp Business API — the no-results wa.me ask is the achievable slice).

### 2026-08-07 · feature · Brand favicon + transactional email (Resend via GroupPad)
- **area:** infra, apps/admin, lib
- **what:** **(favicon)** Cropped the Platter cloche mark out of the logo (PIL, excluding the wordmark) → `app/icon.png` (512) + `app/apple-icon.png` (180); removed the default Next favicon + create-next-app SVG cruft; stashed the full logo at `public/logo.png`. **(email)** Reused GroupPad's Resend account — copied `RESEND_API_KEY` from its Railway project to Platter's (value never printed) + set `MAIL_FROM=Platter <noreply@send.goldhac.com>` (send.goldhac.com is the verified sender). New `lib/notify/email.ts` (Resend HTTP API via fetch — no SDK dep) + a branded `sendInviteEmail`; wired into `inviteTeammate` so invites now **email the join link** (best-effort; link-share still works). Team UI reflects "Invite emailed to X".
- **notes:** deployed + verified (favicon served at /icon.png; Resend key present on Platter). Turns the "real email invites" backlog item live. Emails send from the shared send.goldhac.com domain.

### 2026-08-07 · feature · Multi-venue — active-venue switcher + per-venue admin scoping (verified)
- **area:** apps/admin, lib, db-none
- **what:** A tenant can now own several venues and manage one at a time. **(resolver)** `lib/venue/active.ts` — `getTenantVenues(tenantId)` + React-cached `getActiveVenueId(tenantId)` (the `platter_venue` cookie if it names a venue the tenant owns, else the oldest venue; a stale/foreign cookie can never point outside the tenant). Single-venue tenants are unaffected — active = their only venue, so every added filter is a no-op. **(switch + create)** `lib/mutations/venues.ts` — `setActiveVenue(id)` (validates ownership, sets cookie) + `createVenue(name)` (owner-only: restaurant + default LIVE menu + starter group, inherits currency/tz/locale, switches to it). `<VenueSwitcher>` in the workspace header (hidden < 2 venues) + `<AddVenueButton>` on the dashboard (owners). **(scoping)** Threaded the active venue through the WHOLE admin data layer: the 15 `restaurants … tenant_id … limit(1)` resolution sites now resolve by active id, AND the child-data queries that relied on tenant-RLS alone — the editor tree (`getAdminMenuTree`), `getCategoryOptions`/`getGroupOptions` (also fixed a cross-venue menu-slug collision), `getAdminCategories`, `getModifierGroups`/`getModifierGroupOptions`, the CSV export — now filter by `restaurant_id`. Fixed the theme page's hardcoded `RESTAURANT_SLUG="jin-canting"` → active venue (was theming the flagship for *every* tenant).
- **notes:** typecheck + tokens + queries + prod build green; deployed. **Verified end-to-end** by standing up a throwaway 2nd venue (Test Bistro) under the flagship tenant: data isolation confirmed both ways (flagship 258 items / Test Bistro 1, zero bleed), switcher venue-list + default resolution correct (oldest = flagship), fresh venue rendered live at `/v/test-bistro-tmp` while `/v/jin-canting` stayed intact, and `createVenue`'s inserts confirmed to pass RLS (owner satisfies `auth_is_admin`/`auth_can_manage`). Then removed the temp venue + all test-generated (null-actor) audit rows — flagship back to 1 venue / 258 items / clean feed. The admin switcher UI itself needs a staff login to click through (couldn't self-test — no password), but every layer under it is verified. **Deferred (noted):** dashboard "Recent activity" stays tenant-wide (audit_log has no restaurant_id); `app/sitemap.ts` still flagship-only; no delete-venue UI yet; venue-scoped invites (invites.venue_ids) — the last thin slice, since RLS enforcement is still tenant-level. **Phase A is now essentially clear → the total redesign is next.**

### 2026-08-07 · feature · Phase-A cleanup #1 — marketing pages (/discover · /themes/[id]) + audit log
- **area:** apps/public, apps/admin, db, lib
- **what:** Three of the pre-redesign Phase-A items. **(marketing)** `/themes/[id]` — a per-theme detail page (SSG for all 4 themes via `generateStaticParams`, `notFound` on unknown id): live sample-menu preview per supported scheme (real `resolveTheme` cssVars) + capabilities (schemes · layouts · accent swatches · marks) + cross-links; the `/themes` gallery cards now link into it. `/discover` — public venue directory (`getListedVenues` → `is_listed && active`, anon-readable) with cover/cuisine/blurb cards linking to `/v/<slug>`; added **Discover** to the marketing nav (header + footer). Listed the flagship (`is_listed=true` + cuisine/description + reused the top dish photo as its cover) so the page has content. **(audit log)** migration `0014_audit_triggers` — a **SECURITY DEFINER** `audit_row()` trigger (exception-guarded so it never breaks the write) on items/categories/menus/menu_groups → auto-writes `audit_log` (actor via `auth.uid()`, full before/after jsonb). New `getRecentActivity()` derives human verbs (created · published · removed · marked sold out …) reading the before/after snapshot + resolves actor names; a "Recent activity" feed now sits on the dashboard. Added `lib/format/time.ts` (`timeAgo`).
- **notes:** typecheck + tokens + queries + prod build green; deployed. Verified locally: `/discover` renders the flagship card, `/themes/lacquer` renders the detail, `/themes/<unknown>` → 404, and the audit trigger writes a row on an item touch (confirmed in SQL — actor null under MCP, real staff edits carry the actor). Added `app/discover/` to the token-lint marketing allowlist (brand chrome, like `/themes`). **Remaining Phase-A:** the multi-venue cluster (admin venue switcher + venue-scoped invites) — see the next entry / decision. Then the total redesign.

### 2026-08-07 · feature · Popularity — "Track + Popular section" + Insights screen (verified live)
- **area:** apps/public, apps/admin, lib, db
- **what:** The one Gold asked about — log dish taps → auto "Most popular" shelf + admin analytics. **(db)** migration `0013_popular_items`: a **SECURITY DEFINER** `popular_items(restaurant_id, days, limit)` returning `(item_id, views)` aggregated from `menu_events` (anon can't read raw events, but the aggregate is safe — it's per-public-restaurant, no PII), granted to `anon, authenticated`; + index `menu_events(restaurant_id, event, created_at)`. RLS needed **no change** — `anon_write_events` already lets diners INSERT, `auth_read_events` already scopes staff reads by tenant. **(track)** `app/api/track/route.ts` (POST `{itemId}` → 204) derives `tenant_id`/`restaurant_id` **server-side from the item** (never trusts the client → no cross-tenant spoofing) and inserts one `item_view`. `MenuBoard.open()` fires it via `navigator.sendBeacon` (fetch+keepalive fallback), **skipped when `admin`** so staff don't inflate their own numbers. **(shelf)** `getMenu` calls the RPC in its `Promise.all`, maps ids → live+available items in this menu, returns `popular: MenuItem[]`; `MenuScreen` renders a "Most popular ★" section above the groups when `≥3`. **(insights)** `/admin/analytics` (nav: **Insights**) — reuses `popular_items(…,20)` for a ranked bar list (each row links to the item editor) + two tenant-scoped `menu_events` count headlines (30d / 7d) + empty state. Regenerated `database.types.ts` for the RPC.
- **notes:** typecheck + tokens + queries + prod build green. **Verified live end-to-end** against the local prod build: seeded a realistic 96-event long-tail across 8 signature dishes → RPC returned the correct top-6 → "Most popular ★" shelf rendered in order (Geogold Fried Rice · Chicken Shawarma · Chicken Samosa …) → a real in-browser tap opened the sheet AND wrote a fresh `item_view` (`session_id NULL`, **tenant_id correctly derived**) confirmed in SQL. Seed events carry `session_id LIKE 'seed-%'` — remove with `delete from menu_events where session_id like 'seed-%'` to reset the flagship to real-only. **Phase-A remaining:** multi-venue switcher · marketing pages (`/discover`, `/themes/[id]`) · team polish (audit log · venue-scoped invites · email stub). Then the total redesign.

### 2026-08-07 · perf · Lazy-load the item sheet — anon menu back under budget (218→205KB)
- **area:** apps/public
- **what:** The detail sheet (`ItemSheet`) statically pulled **`@radix-ui/react-dialog`** (~13KB gz) onto the anon menu's **critical first-load**, even though it's empty until a diner taps. Code-split it: `MenuBoard` now `dynamic(() => import("./item-sheet"), { ssr:false })`, mounted only from the first open onward (immediately for a deep-linked item), and **warmed on `requestIdleCallback`** so the first tap stays instant. Clears the queued "+2–3KB over" overage (`foundation.md §7 #6` / CLAUDE.md #4) and speeds first paint for **every** customer.
- **notes:** `measure-bundle` (against a local `next start`) **205 KB / 215 budget ✓** (was 218). Verified in-browser: tap → sheet opens from the lazy chunk, back/forward still syncs, deep-linked item URL still renders the sheet. Radix now loads off the critical path (idle prefetch or on first tap) — no perceptible tap latency.

### 2026-08-07 · feature · Wave 2.5 — Onboarding wizard (Details → Link → Menu)
- **area:** apps/admin, lib
- **what:** Turned the single-step onboarding (just the AI import) into a guided 3-step wizard. `components/admin/onboarding-wizard.tsx` (progress bar + Back/Continue): **Details** (venue name · cuisine · currency → new `saveOnboardingBasics` action), **Your link** (subdomain slug with `<host>/v/…` preview → reuses `updateSubdomain`), **Your menu** (the existing `MenuImport` + "Go to dashboard"). The onboarding page still provisions the tenant first (idempotent `provision_tenant`), then prefills the wizard from the venue's row.
- **notes:** typecheck + tokens + queries green; deployed. Wave 2 core done (Generate ✓ · Touch-up ✓ · dashboard usage bar ✓ · publish-all-drafts ✓ · move/duplicate already existed ✓ · onboarding ✓). **Phase-A features remaining:** popularity "Track + Popular section" (needs event-logging) · multi-venue switcher · marketing pages (`/discover`, `/themes/[id]`) · team polish (audit log · venue-scoped invites · email stub). Then the total redesign.

### 2026-08-07 · feature · Wave 2.2/2.3 — Touch-up AI button + dashboard plan-usage bar
- **area:** apps/admin, lib
- **what:** **(2.2) "✨ Touch up" button** (`ImageUpload`, shown when a photo exists) — image-to-image enhance of the *uploaded* photo (Gold's ask): new `touchUpItemImage(imageUrl)` server action → MuAPI **nano-banana-pro** edit endpoint (`{image_url, prompt}`, "enhance lighting/colour, tidy background, keep the dish") → sharp → upload. Refactored `generate-image.ts` to share `submitAndPoll()` + `finalize()` between generate + touch-up. **(2.3) Dashboard plan-usage bar** — `Free/Pro` badge + "menus X / limit" progress bar + an "at your limit → upgrade to Pro" nudge (from `lib/plans` + a `tenants.plan` lookup); Manage → billing.
- **notes:** typecheck + tokens + queries green; deployed. Touch-up uses an instruction-edit model (preserves the dish) vs seedream img2img (would over-restyle). Not yet live-verified (shares the generate path, which IS verified — only the endpoint/prompt differ). Wave 2 remaining: onboarding wizard. **(2.4) Editor "Publish N drafts" button** added — `publishMenuDrafts(menuId)` (manager+, scoped via groups→categories) + `PublishAllButton` in the menu-editor header (hidden at 0 drafts); move/duplicate already existed (`bulkMoveCategory` + `duplicateItem`). Then Wave 3 + the redesign.

### 2026-08-07 · feature · Wave 2.1 — ⭐ "Generate with AI" button in the item editor (Gold's ask)
- **area:** apps/admin, lib, infra
- **what:** Self-serve version of the bulk photo backfill: a **"✨ Generate with AI"** button in `ImageUpload` (shown whenever the form passes a name — item editor feeds it `watch("name")`/`watch("description")`). New server action `lib/mutations/generate-image.ts` — `requireManager()` gates the paid call → MuAPI **HTTP API** (`x-api-key`, `POST bytedance-seedream-v4` → poll `/predictions/{id}/result`, ~22s) with the locked dark-fine-dining style → download → **`sharp`** resize to 1000px/q72 JPEG → upload to `menu-images/{tenantId}/gen-<uuid>.jpg` on the **caller's session** (storage RLS, same path as manual upload) → returns the public URL into the form. Optimistic UX: `toast.promise` "Generating… (~25s)". Added `sharp` dep (server can't use the local macOS `sips`), set **`MUAPI_API_KEY` in Railway** (the deployed server can't use the local `muapi` CLI).
- **notes:** typecheck + tokens + queries green; deployed. Spend guard = manager+ only (each generation ~$0.04). The item-editor "Generate" reuses the **HTTP** path (async submit+poll), *not* the CLI/sips path the bulk `scripts/generate-item-images.mjs` used — that one is macOS-local-only. **Verified live (2026-08-07):** `/admin/items/new` → typed "Grilled Lamb Skewers" → Generate → "Photo generated" toast + a real dark-style skewers photo in the slot (whole MuAPI-HTTP → sharp → storage-upload path works on Railway). **Next (Gold's ask):** a **"Touch up with AI"** sibling button — image-to-image enhance of an *uploaded* photo (same plumbing, MuAPI edit endpoint). Backlog v2: regenerate · style-preset picker · margin-aware prompt.

### 2026-08-07 · feature · UX/perf Wave 1 — loading, errors, toasts, crossfade, optimistic, anon-guard
- **area:** apps/public, apps/admin
- **what:** Killed the "feels static / laggy on click" issues + a bug, from a UX audit (`docs/UX-POLISH.md`). **Loading:** `app/(public)/loading.tsx` (menu skeleton) + `app/(admin)/admin/(workspace)/loading.tsx` — navigations show an instant shimmer, never a frozen blank. **Errors:** `app/(public)/error.tsx` (branded "Something went off — Reload") + root `app/not-found.tsx` — no more raw framework error screen. **Toast bug fixed:** `<Toaster>` was only in the admin layout, so the admin-on-menu drawer's toasts silently dropped — mounted one **inside `AdminLayer`** (admin-scoped, in the dynamic chunk, zero anon cost). **Image crossfade:** `next/image` `placeholder="blur"` (shared `BLUR_DATA_URL`) on `ItemThumb` + `item-sheet` — photos fade in instead of popping from empty boxes. **Optimistic drawer:** quick-edit Save now closes instantly + `toast.promise` runs the mutation + refresh in the background (no "Saving…" freeze); sold-out/publish same. **Anon fast-path:** `menu-screen` skips `getCurrentStaff()` unless a Supabase `sb-…auth` cookie is present, so logged-out visitors don't pay for the staff lookup.
- **notes:** typecheck + tokens + queries green; deployed. **Still open from the UX list** (`docs/UX-POLISH.md`): top nav progress bar · admin-list skeletons · destructive-action confirms · long-op progress toasts (image-gen/import) · form inline validation · focus-trap on sheet/drawer · full client-side optimistic state (this pass is instant-response + background-sync, not yet local-rollback). **Perf still to do:** cache the customer menu (drop `force-dynamic`) + the 2KB bundle trim. This is Wave 1 of the "knock everything off then redesign" push Gold kicked off — features next (Generate button · onboarding wizard · editor bulk actions · dashboard · analytics · multi-venue · marketing · team polish), then a total redesign.

### 2026-08-07 · feature · Admin-on-public-menu layer (front-of-house editing) — Gold's ask
- **area:** apps/public, apps/admin, lib
- **what:** When signed-in staff view their own venue's public menu, it becomes a management surface (Gold: "logged in as admin it should be different"). **Server-gated** by `getCurrentStaff()` in `menu-screen.tsx` — admin only if the viewer is staff of *this venue's tenant* (`staff.tenantId === restaurant.tenant_id`; `getMenu` already runs on their cookie session so RLS includes their drafts). The layer (`components/menu/admin/*`): **fixed bottom admin bar** (+ Add item · Edit menu · Dashboard · **View-as-customer** toggle), **inline edit pencil** on every item (delegated through MenuBoard's existing click handler via `data-edit-slug` — zero per-item JS), a **quick-edit slide-over** (name · price · sold-out · publish/hide, with a "Full editor →" deep-link — server actions `quickUpdateItem` [new] + `toggleItemAvailability` + `setItemStatus`), and **draft items shown to admins** dimmed with a "Hidden" badge (invisible to customers via RLS). The whole layer is a **`next/dynamic` import** so customers download none of its JS; "view as customer" hides it all via a `[data-admin-preview]` CSS switch in globals.css. `getMenu` now selects `tenant_id` + item `status`.
- **notes:** Choices (Gold): **full layer** + **quick-edit drawer** (vs deep-link). Gates green after clearing macOS/iCloud "… 2" sync-duplicate files from `.next/types` (this repo lives under synced `Documents` — those dupes recur + break `tsc`; `find .next -name "* [0-9].*" -delete`). **Verified live (2026-08-07):** on the owner's session the bottom admin bar renders (+Add·Edit menu·Dashboard·Preview) and the pencil opens the quick-edit drawer pre-filled (name/price/sold-out/hide/Full-editor); anon menu unregressed. **First deploy crashed the logged-in path** — `adminInfo.itemHref` was a **function passed as a prop to a client component**, which RSC forbids ("Functions cannot be passed directly to Client Components"). typecheck can't see it + the menu is dynamic so it only surfaced with a real session (not at build) — fixed by passing `itemHrefBase` (string) and composing the href in the client. **Lesson:** no functions across the server→client prop boundary. **Bundle regression (`measure-bundle`): menu 204 → 217KB, 2KB over the 215 budget** (floor `/pricing` unchanged at 174KB). Marker-checked the anon chunks: the heavy admin bits (quick-edit drawer, sonner, bar, `data-admin-preview`) are correctly **code-split out** — the residual is the small always-loaded pieces `menu-board` statically imports (the pencil `ItemAdminControls` + the `admin-context` hook) plus Turbopack re-chunking. **Trim queued into the perf pass** Gold flagged (optimistic UI · guard `getCurrentStaff` for anon · cache the customer menu · get these last KB back out of the anon bundle, e.g. lazy the pencil too / drop the `dynamic()` loader overhead). No drafts exist yet (all 258 imported items are published), so the "Hidden" badge has nothing to show until a new item is added (new items default to draft). Pencil sits top-right, overlapping the list-dense thumbnail corner — fine for v1, revisit if it reads cramped.

### 2026-08-07 · feature · Flagship food photos — 175 AI images generated + wired LIVE
- **area:** apps/public, infra, scripts
- **what:** Every **Dinner food item (175/175)** now has an AI-generated photo, live on the menu. Pipeline (`scripts/generate-item-images.mjs`, resumable — skips items with an `image_url`): seedream-v4 via the **muapi CLI** (self-auths from its own config; `muapi run seedream-v4 -p … -j`) → `sips` resize to 900px/~160KB JPEG → upload to the **`menu-images`** bucket via the **service-role key** (Gold pasted it into `.env.local`; the app itself still uses none) → set `items.image_url` to the public URL. One locked "dark fine-dining" style prompt → cohesive set. Ran 4-wide, **0 failures, ~$6.92** (balance ~$31). The public menu is dynamic + the Supabase host is already in `next.config` `images.remotePatterns`, so photos appeared with no deploy. Verified 175/175 have images, storage URLs 200, next/image returns valid JPEGs, thumbnails render on the `list-dense` (Lacquer) layout.
- **notes:** **Known flaw (fix pending Gold's call):** the prompt passed the category in parens (`Name (Appetizers)`), and seedream-v4 sometimes **rendered that as a text caption** on the image (confirmed on "Chicken and Chips"); frequency unaudited (browser hung). Fix = regen all 175 with the category dropped + hardened no-text (~$7), or targeted. **Gold's call (2026-08-07): keep as-is** — occasional captions accepted, no regen. (The item-editor "Generate" backlog feature should still drop the category + harden no-text so future one-offs are clean.) **Bar List (83 drinks) skipped** — Carafe `ruled-list` is text-only. Raw muapi outputs are 4K/4.7MB + URLs expire in 30 days → we re-host resized copies in Supabase, not link muapi directly. **Backlog holds** the "Generate button in the item editor" feature (same pipeline, self-serve) from Gold's ask.

### 2026-08-06 · feature · M10 CI gates complete (6/6 wired) + a11y `<main>` fix
- **area:** infra, apps/public
- **what:** Finished wiring all six M10 gates as `npm` scripts. Added: **`test:isolation`** (`scripts/test-isolation.sh` — runs `tests/isolation.sql`'s 5 checks via psql against `$DATABASE_URL`, with a **prod-refusal guard** since it seeds test tenants; needs the `railway connect Postgres` tunnel to run); **`gate:a11y`** (`scripts/gate-a11y.sh` — `@axe-core/cli`); **`gate:lighthouse`** (`scripts/gate-lighthouse.sh` — Lighthouse ≥90 across perf/a11y/best-practices/seo, parsed in node). All three syntax-checked; the two headless-Chrome ones are on-demand/CI (heavy, not in the fast `gates` suite). **a11y finding + fix:** ran a real WCAG probe against the live menu via the in-app browser — clean except **no `<main>` landmark** (axe `landmark-one-main`/`region`); fixed by making the menu content root a `<main>` in `components/menu/menu-screen.tsx` (shared by `/menu` + `/v/<slug>`). Deployed.
- **notes:** Full gate roster: `lint:tokens` (no-raw-color ✓) · `lint:queries` (no-unscoped-query ✓, negative-tested) · `measure-bundle` (JS ≤215KB ✓, menu 204KB) · `test:isolation` (P0 — CI-ready, run on the dev-DB tunnel) · `gate:a11y` + `gate:lighthouse` (need Chrome). `npm run gates` = the fast static trio (typecheck + tokens + queries). The a11y probe also confirmed the real-data menu is otherwise clean: lang set, single h1, no heading skips, all 196 interactive elements have accessible names, the search input is labeled, no positive tabindex, no dup ids. **M10 remaining:** run `test:isolation` on the tunnel + `gate:lighthouse`/`gate:a11y` once (record the numbers) · load test (100 tenants/20k items) · rate-limits · transactional email · the deferred perf items + `0010`/`0011` migration-file reconstruction.

### 2026-08-06 · decision · JS budget re-baselined 120KB → 215KB (Gold's call)
- **area:** context
- **what:** Resolved the budget finding below. `foundation.md §7 #6` + P8, `CLAUDE.md` guardrail #4, `architecture.md`, `library-docs.md`, `docs/BUILD-CHECKLIST.md`, and `scripts/measure-bundle.sh` default all moved **120KB → 215KB** initial JS. Verified: the flagship menu (204KB) now passes `measure-bundle.sh`.
- **notes:** Rationale (measured, not guessed): the React 19 / Next 16 (Turbopack) framework floor is ~174KB gzipped first-load on a blank page, so 120KB was unreachable regardless of app discipline. 215KB = floor + the menu's lean ~30KB app delta + headroom. The **app-discipline** signal still holds via the other gates (`lint:queries`, no heavy-dep leaks, `'use client'` justification) — if a future route balloons past 215KB, the gate catches it. Considered but not chosen: a "delta over floor ≤40KB" gate (more principled, more complex) and a floor-shrink investigation (uncertain payoff). Lighthouse ≥90 remains the other half of the perf gate (unchanged).

### 2026-08-06 · feature · M10 CI gates — no-unscoped-query + bundle gate (+ a budget finding)
- **area:** infra, context
- **what:** Built two of the six M10 CI gates as scripts matching the `lint-tokens.sh` pattern, wired into `package.json` (`lint:queries`, `measure-bundle`, + a `gates` aggregate = `typecheck && lint:tokens && lint:queries`). **(1) `scripts/lint-queries.sh`** — the "no unscoped query / no RLS bypass" gate. Since isolation here is 100% RLS (no service-role client anywhere), it fails the build on: a service-role ref in `app/lib/components`; a raw `@supabase/supabase-js` client import outside the wrappers; `createServer/BrowserClient` outside `lib/supabase/{server,browser,session}.ts`; or a `'use client'` file importing the server client. Green on current code, and **negative-tested** (a throwaway file with a service-role client + raw SDK import correctly fails it). **(2) `scripts/measure-bundle.sh`** — the "≤120KB initial JS" gate; measures a route's gzipped first-load `<script>` chunks over the wire.
- **notes:** **Bundle finding (needs a foundation call):** the 120KB first-load-JS budget (`foundation.md §7 #6`, guardrail #4) is **structurally unmet by this stack.** Measured live: static marketing routes `/pricing` + `/themes` = **174KB gzipped floor** (≈zero app code — this is the React 19 + Next 16/Turbopack framework baseline), flagship menu = **204KB**. So the app code is *lean* (+~30KB over floor; clean splitting — **no heavy dep leaks into the menu**, only 3 `'use client'` components: category-rail, menu-board, item-sheet; root layout is a pure server component), but the absolute floor is 54KB over budget. Options for Gold: re-baseline the budget (~185KB absolute, or reframe as "≤~30KB app-JS over the framework floor"), or accept the framework floor as fixed. `next build` on Turbopack no longer prints the Size/First-Load-JS columns, so the wire measurement is the number. **Remaining M10 gates:** isolation wrapper (needs the dev-DB tunnel) · Lighthouse ≥90 · axe · no-raw-color (`lint:tokens` ✓).

### 2026-08-06 · chore · M10 (start) — security review + hardening migration `0012`
- **area:** db, infra
- **what:** Re-ran the Supabase advisors (the M10 "re-run advisors" line item) and applied `0012_m10_hardening.sql`. **Security:** the one real gap — `enforce_menu_plan()` is a *trigger* fn but still had the default `PUBLIC` EXECUTE grant, so it was reachable at `/rest/v1/rpc/enforce_menu_plan` by anon+authenticated (advisors 0028/0029). Revoked → ACL now `postgres | service_role` only (matches the sibling `enforce_staff_*` triggers); trigger `trg_menus_plan` still fires (EXECUTE grants are orthogonal to trigger firing). **Perf:** added `idx_categories_group` on `categories(group_id)` — the public menu tree joins groups→categories every render and the FK was uncovered (advisor 0001).
- **notes:** The remaining advisor WARNs are **expected/accepted, not bugs:** the `auth_*` helpers must stay `authenticated`-executable (RLS policies invoke them; each only returns the *caller's own* tenant/role), and `accept_invite`/`provision_tenant` are the intended onboarding/invite RPCs. Deferred (low-impact at current scale, or dashboard-only): `memberships.auth_read_members` initplan `(select auth.*)` rewrite · `multiple_permissive_policies` (read + `FOR ALL` write policies overlap on SELECT — a careful RLS refactor) · `pg_trgm` out of `public` · **Auth leaked-password protection (Gold: dashboard toggle)** · unused indexes (pre-launch, expected). **Repo drift found:** migrations `0010`/`0011` were applied via MCP and exist in the DB history but **not as local files** (only 0012 was written to `supabase/migrations/`) — reconstruct them from the live fn defs so the repo can rebuild from scratch. **M10 still open:** the 6 CI gates (isolation wrapper · JS ≤120KB · Lighthouse ≥90 · axe · no-raw-color[exists] · no-unscoped-query), load test (100 tenants/20k items), rate-limits, transactional email.

### 2026-08-06 · feature · Flagship real data — 258 scraped Jīn Cāntīng items imported LIVE
- **area:** db, apps/public
- **what:** Replaced the 14-item placeholder seed with the **real scraped menu** (`scripts/legacy-menu.csv`, 258 items / 19 categories). Built the proper hierarchy under the flagship's two existing menus: **Dinner (`menu`)** → 3 groups (Starters, Mains, Rice/Noodles & More) / 14 categories / **175 items**; **Bar List (`bar-list`)** → 2 groups (Soft, Wine & Spirits) / 5 categories / **83 items**. Prices imported as scraped (₦-native), descriptions preserved (whitespace-normalized), `status='published'`, `spice/tags/allergens` left at defaults. Generators + import SQL in `scratchpad/` (`gen-json.mjs` → dollar-quoted `jsonb_to_recordset` chunks); `scripts/import-legacy-items.mjs` written for the supabase-js path but **unused** (no service-role key locally or in Railway — app is pure anon+RLS), so the load ran through the Supabase MCP admin connection instead.
- **notes:** Verified live — `/v/jin-canting` renders all 14 dinner categories with real items + descriptions + `₦` prices (e.g. Chicken Samosa ₦6,000, Fried Lobster ₦19,500); `/v/jin-canting?m=bar-list` renders the bar (Heineken, Hennessy, Chapman, smoothies). SQL verify: 258 published, 0 orphans, both menus `live`, seed **soft-deleted** (14 archived rows — no hard delete, per guardrail #5; seed category/group slugs suffixed `-arch-<id>` to free the real slugs). **Follow-up (Gold): the 26 Wine & Spirits + several Drinks scraped as `₦1`** (source had no price) — they show "₦1" on the live menu; need real prices entered in the admin (or hide until priced). One dupe scrape kept with a `-2` slug (two "Calemari with Curry Sauce", two "Ice Cream"). Menu-switch is via `?m=<slug>`; the `/v/<slug>/bar-list` **path** segment falls back to the primary menu (pre-existing routing — path switches categories, not menus).

### 2026-08-05 · deploy · M7 — `platter.goldhac.com` LIVE (custom domain on Railway+Cloudflare)
- **area:** infra
- **what:** The app now serves on **`platter.goldhac.com`** over HTTPS. Domain structure locked: `goldhac.com` = Gold's umbrella Cloudflare zone → Platter at `platter.goldhac.com`, venues to come at `<slug>.platter.goldhac.com`. Set up **via Chrome browser-automation** (the Railway CLI `domain` op returns `Unauthorized` even re-authed — a known quirk, dashboard works): added the Railway custom domain (port 8080) + the two Cloudflare records — **CNAME `platter` → `lyidrllh.up.railway.app`** and **TXT `_railway-verify.platter`** — both **DNS-only (grey cloud)**, per the proven GroupPad recipe ([[railway-cloudflare-domain-recipe]]). Railway verified + issued the Let's Encrypt cert ~4 min later (clean, no stuck state). Set `NEXT_PUBLIC_SITE_URL=https://platter.goldhac.com` + rebuilt to bake it into canonical/OG.
- **notes:** Verified live: `curl https://platter.goldhac.com` → `HTTP/2 200`, `server: railway-hikari` (direct to Railway, grey-cloud confirmed), serving the marketing home. Gotcha that cost time: the long TXT value **jammed the Cloudflare add-record dialog when typed** — fixed by `pbcopy` + Cmd-V paste. **Next domain step (deferred):** the `*.platter.goldhac.com` **wildcard** for venue subdomains (needs a Railway wildcard domain + a DNS-01 TXT challenge) → then set `NEXT_PUBLIC_PLATFORM_DOMAIN=platter.goldhac.com` and `<slug>.platter.goldhac.com` resolves per venue. The Railway apex URL still serves too (flagship QR back-compat).

### 2026-08-05 · feature · QR Studio (M7) — bulk table codes + print export
- **area:** apps/admin, lib
- **what:** Upgraded the basic QR tool into **QR Studio** (`components/admin/qr-studio.tsx`, replaces the removed `qr-tools`): pick the target (whole venue **or a specific menu** → `?m=`), generate **single or bulk table codes** (tables 1–N, capped 100), choose **durability** (error-correction High/Standard), and export **PNG · SVG · A6 table-tent PDF · A4 sheet PDF** — the sheet is a labelled grid of every table's code (the bulk-print headline). `lib/table-tent.tsx` is now venue-parameterized (was hardcoded "JIN CANTING") and **ASCII-folds** macrons for @react-pdf's Latin-only font (`Jīn Cāntīng` → `Jin Canting`, verified); added `generateQrSheetPdf`. The QR page now uses `getAdminMenus`. **Per-table scan analytics is free from GA** — codes carry `?t=`/`?m=`, so GA's top-pages report segments by table + menu (the UI says so).
- **notes:** Build + token gate green; old `qr-tools.tsx` removed (only self-referenced). PDF gen is client-side (@react-pdf, dynamically imported → code-split off the initial bundle). Codes point at `/v/<slug>` today; the UI tells staff to regenerate on the branded subdomain once DNS lands. **Not browser-tested** (admin auth-gated + local dev resource-flaky) — Gold prints one to confirm the PDFs. Deferred `[B2]` in-theme menu print PDFs (A4/A3/tent/specials) — the QR sheet + tent cover the QR side.

### 2026-08-05 · feature · M9 (slice 2) — Google Analytics + import landing
- **area:** apps/public, marketing
- **what:** GA4 wired via `components/analytics/google-analytics.tsx` (next/script, `afterInteractive` — no dependency), rendered in the root layout **only when `NEXT_PUBLIC_GA_ID` is set**, so it goes live the moment Gold drops the `G-…` id into Railway env. The page path carries the venue (subdomain / `/v/<slug>`), so GA's top-pages report doubles as per-venue view counts until in-app analytics lands. Root metadata made **Platter-generic** (was flagship-specific; per-page metadata still overrides). Added `/menu-import` — a focused landing for the AI import (3-step how-it-works + CTAs).
- **notes:** Lint + build green. GA loads globally incl. `/admin` (staff traffic is small + filterable in GA); per-tenant in-app analytics (needs custom event-logging on the unused `menu_events`/`qr_scans`) stays deferred. **Domain decided: `goldhac.com` is Gold's umbrella for all apps → Platter lives at `platter.goldhac.com`, venues at `<slug>.platter.goldhac.com`.** The Railway custom-domain add is blocked (CLI "Unauthorized" — custom domains are a paid Railway feature / dashboard action, not a token issue); retrying per Gold. Env (`NEXT_PUBLIC_PLATFORM_DOMAIN`/`SITE_URL`) held until DNS resolves.

### 2026-08-05 · feature · M9 (slice 1) — marketing site (home · pricing · themes)
- **area:** apps/public (marketing)
- **what:** The platform apex now serves a real marketing site (was a bare redirect to `/menu`). `/` — a hero with a **theme-cycling phone mockup** (the "one menu, four looks" differentiator, `components/marketing/hero-cycler.tsx`), features, a theme showcase, a pricing teaser, and CTAs to `/admin/signup`. `/pricing` — Free (₦0) vs Pro comparison built from `lib/plans` (Pro price = "coming soon", checkout parked). `/themes` — the four themes rendered **in-theme** (real `resolveTheme` cssVars) with tagline + best-for. Shared `MarketingShell` (header/footer). `/` is **host-aware**: a venue subdomain/custom-domain → redirect to `/menu`; the apex → the marketing home.
- **notes:** Token gate + build green. Extended `scripts/lint-tokens.sh` EX so the marketing files may use the fixed Platter palette (same rationale as the admin chrome) — the themed swatches inside them still use semantic tokens. **Local dev render couldn't be verified — machine resource-starved again** (concurrent CSV-fix session + MCP servers); verifying on the live URL post-deploy. **Analytics decision:** use **Google Analytics (GA4)** rather than custom event-logging (`menu_events`/`qr_scans` are unused) — Gold has GA open, will supply the Measurement ID. **Domain:** `goldhac.com` (Cloudflare, unused) — Gold has Cloudflare open; wiring it next. Deferred M9: `/discover`, `/menu-import` landing, digest email (Resend), `B3/B4/B7`, `/themes/[id]`.

### 2026-08-05 · feature · M8 — plan & billing page (checkout deliberately stubbed)
- **area:** apps/admin, lib
- **what:** `/admin/billing` (a dashboard quick-action) — shows the current plan (Free/Pro), live usage (menus · team seats · custom domain), and a Free-vs-Pro comparison rendered from `lib/plans` (the one source). Gives the plan-limit "upgrade to unlock" errors a home. The **"Go Pro" CTA is a deliberate stub** ("card checkout coming soon") — Gold chose to leave the actual Paystack setup for later. `lib/queries/admin-billing.ts` (getBilling), `components/admin/billing-view.tsx`.
- **notes:** Token gate + `tsc` + build all green — but only after a transient hang: the machine was **resource-starved** (this session + the concurrent CSV-fix session + a stack of MCP servers), so `next build` and even `tsc --noEmit` timed out repeatedly until things went idle, then built in ~4s. Kept the billing query simple (no conditional-in-`Promise.all`) to cut type risk while local checks were unreliable. **M8 status: enforcement ✓, team ✓, plan visibility ✓ — only the real Paystack checkout + signed webhooks remain, parked pending Gold's Paystack account.**

### 2026-08-05 · feature · M8 (slice 2) — team invites (link-based, seat-gated)
- **area:** apps/admin, db, lib
- **what:** `/admin/team` (owner): member list + pending invites + an invite form (email + role staff/manager), revoke, and remove-member — all **seat-gated** via `canAddTeamMember` (Free = owner-only, Pro = 10 seats). Invites are **link-based** (no email infra): `inviteTeammate` writes an `invites` row (24-byte token, 14-day TTL) and returns a **shareable join link**. `/admin/join?token=` (`components/admin/join-flow.tsx`) handles both auth states — sign in/up with the invited email, then accept — through `accept_invite(token)` **SECURITY DEFINER** (migration `0011`): validates the token (unexpired, unused, email matches the caller, caller not already on another team) → writes a membership + staff row for `auth.uid()`. The Proxy now exempts `/admin/join` (reachable signed-in or out). Nav gains "Team".
- **notes:** Build + token gate green. `accept_invite` verified live: raises on a bad token, executable by `authenticated` but **not `anon`**. Member list works because `auth_read_staff` scopes SELECT to the whole tenant (not just the caller). `lib/mutations/team.ts` guards: owner-only, can't remove yourself or an owner, no duplicate pending invite. The full invite→accept round-trip needs **two real accounts** — Gold's test. Deferred: role-escalation confirm + audit log, venue-scoped invites (`invites.venue_ids`), real email delivery. **M8 remaining: Slice 3 — Paystack checkout, which needs Gold's Paystack account + keys.**

### 2026-08-05 · feature · M8 (slice 1) — plan-limit enforcement (three layers)
- **area:** lib, db, apps/admin
- **what:** `lib/plans.ts` — the single source of plan limits (**Free**: 1 menu/venue, Lacquer only, no custom domain, owner-only, Platter branding; **Pro**: unlimited menus, all themes, custom domain, 10 seats, no branding). Enforced in **three layers**: (1) **UI** — the customiser now reads `allowedThemes(plan)` instead of an inline rule (Pro locks/badges); (2) **server mutations** — `createMenu` + `commitParsedMenu` gate `maxMenus`, `publishMenuTheme`/`saveMenuThemeDraft` gate `canUseTheme`, `updateCustomDomain` gates `canUseCustomDomain`; (3) **Postgres trigger** `enforce_menu_plan` (migration `0010`) backstops menu-count + Lacquer-only on `menus`. Set the flagship tenant (De Geogold Hotel) to **Pro** first, since it runs 2 menus + the Carafe theme.
- **notes:** Build + token gate green. **DB trigger verified live** on a throwaway Free tenant inside a self-cleaning `do` block: 2nd menu → blocked, Carafe → blocked, rows removed (confirmed 0 residue; flagship = pro). Keep the SQL Free limits in `0010` in sync with `lib/plans.ts`. Pricing (₦/mo) is still TBD — the limits enforce; pricing is a marketing number. **Slice 3 (checkout) needs Gold's Paystack account** — the checklist's Paystack(NGN)+Stripe(USD) plan holds; Stripe won't onboard a Nigerian business, so Paystack is the NGN path. Remaining M8: team invites (slice 2), billing/upgrade UI + Paystack webhooks (slice 3).

### 2026-08-05 · feature · M7 (slice 2) — public-address claim UI + per-venue OG
- **area:** apps/admin, apps/public, lib
- **what:** `/admin/domains` ("Public address" — new nav item + dashboard quick-action): shows the venue's live `/v/<slug>` URL (copy/open), lets the **owner change their subdomain/slug** (validated: format + the reserved-list from `resolve.ts` + cross-tenant uniqueness) and **set a custom domain** with the Railway + DNS steps spelled out. `lib/mutations/domains.ts` — owner-only `updateSubdomain` / `updateCustomDomain`. Made **`/api/og` venue-aware**: `?r=<slug>` renders any venue with its own name, initial mark, and **theme colours** (via `getMenu` + `resolveTheme`); `buildMenuMetadata` now emits per-venue OG for every venue (was flagship-only, hardcoded jin-canting branding).
- **notes:** Build + token gate green. **Local-verified:** `/api/og` and `/api/og?r=…&item=…` → 1200×630 `image/png` (screenshot confirms the venue name, lacquer-red accent square + wordmark, and address); `/v/` metadata references `?r=<slug>`; `/admin/domains` auth-gates cleanly (307, no 500). Slug-change **warns to reprint QRs** — no redirect plumbing yet, so a change orphans already-printed links (follow-up: 301 via the existing `redirects` table). Custom-domain here only records host→venue; it also needs adding in Railway + DNS to actually route. Remaining M7: QR Studio (bulk per-table + scan analytics), custom-domain verify, `venue:{host}` tag caching, and the joint DNS activation. **(Aware the CSV-orphan-categories fix is running in a separate session — left `import.ts`/`csv-import.tsx` untouched.)**

### 2026-08-05 · feature · M7 (slice 1) — multi-venue public menu (host→venue + `/v/<slug>`)
- **area:** apps/public, lib
- **what:** The public menu is no longer hardcoded to `jin-canting`. `lib/venue/resolve.ts` maps `host` → venue (exact custom_domain → `<slug>.<PLATFORM_DOMAIN>` subdomain → apex falls back to the flagship), cached per request, with a reserved-slug set. Extracted the render into `components/menu/menu-screen.tsx` + metadata into `lib/menu/metadata.ts`, both `basePath`-parameterized. Two thin routes now share them: `/menu/[[...slug]]` (host-resolved; apex → flagship, so printed-QR links never break) and the **new `/v/[venue]/[[...slug]]`** (path-based — a public URL that works on any host today, the shareable link a venue gets before it claims a subdomain). Threaded `basePath` through MenuBoard's shallow routing (open/close/popstate) **and** the four layout Item `<a href>`s, so item deep-links stay under the right venue.
- **notes:** RLS-checked before building — `anon_read_restaurants` is `true` (any venue resolves by slug/custom_domain), menus/items are public only when published. **Local-verified end-to-end:** `/menu` = jin-canting byte-unchanged (flagship QR back-compat); `/v/jin-canting` renders with venue-correct `/v/jin-canting/...` hrefs (0 `/menu/` leakage), working deep-link SSR (`role="dialog"`), correct canonical; `/v/does-not-exist` → 404. Build + token gate green. **Subdomain + custom-domain resolution is code-complete but dormant until DNS** — Gold buys `platter.menu`, adds it + `*.platter.menu` in the Railway dashboard, points DNS, then sets `NEXT_PUBLIC_PLATFORM_DOMAIN` + `NEXT_PUBLIC_SITE_URL` on Railway and subdomains light up with zero code change. Also made the admin **venue-aware** so it's coherent for non-flagship tenants: dashboard + menus "View" links now open `/v/<restaurant-slug>` (were `/menu` = flagship), and the **QR generator** encodes the venue's own `/v/<slug>` (was a hardcoded `/menu`, which would have printed the flagship's menu on another restaurant's QR). Deferred follow-ups: per-venue `/api/og` (flagship-only now), `venue:{host}` tag caching (still `force-dynamic`), and the in-app subdomain/custom-domain **claim UI** (slice 2).

### 2026-08-05 · feature · M6 — self-serve onboarding funnel (signup → provision → import)
- **area:** apps/admin, db, lib
- **what:** A logged-out restaurant can now sign itself up and land with a working, isolated tenant — no manual seeding. Parts: migration `0009_onboarding_provision.sql` — `provision_tenant(p_name)` **SECURITY DEFINER** (creates tenant → restaurant → owner `staff` row → owner `membership`; idempotent; unique slug free in both tenants+restaurants; reads the business name from signup metadata; **granted to `authenticated` only**, no service-role key). `/admin/signup` (client, Platter-branded — restaurant name + email + password, stores name in `auth` metadata, handles both immediate-session and email-confirm paths). `/admin/onboarding` (server — provisions idempotently for whoever just authed, then leads with the **M6 `MenuImport`** + a "skip" to the dashboard). Proxy (`session.ts`) now allow-lists `/admin/signup` for logged-out access; login links to signup. Regenerated DB types for the new RPC.
- **notes:** Build + token gate green; `/admin/signup` + `/admin/onboarding` compile. Verified the DDL live: `provision_tenant` raises `not authenticated` with no JWT and is executable by `authenticated` but **not `anon`**; no blocking triggers on the tenancy tables (only `set_updated_at` on UPDATE), so the definer inserts run clean. **Not built yet (deferred):** the account creation itself is real auth — I did **not** create a test account (that's the user's to do); Gold runs the first real signup. New tenants have no public URL until **M7** (the public `/menu` still hardcodes `jin-canting`); their menu lives in the admin + the customiser's live preview until host→venue routing lands. Currency defaults to NGN for new venues (settings can change it) — a wizard currency step is a follow-up. If Supabase email-confirmation is ON, signup shows "check your email" then the link → `/auth/callback?next=/admin/onboarding`; toggle it off in Auth settings for an instant demo path.

### 2026-08-05 · decision+docs · M7 stays on Railway (Vercel cutover dropped)
- **area:** context
- **what:** Reversed the 2026-08-04 "migrate to Vercel at M7" lock (`§13 P-Q2`). M7 will do custom **and** wildcard domains **on Railway** (CNAME + wildcard cert) — no migration. Railway is proven across M1–M6 and supports both; staying avoids a second host + the Vercel Pro spend. Vercel's only real edge (global edge-latency + a slicker domain-attach API) is deferred as an optimization.
- **notes:** Prompted by Gold ("isn't M7 going to be Railway now?"). Updated `foundation.md §13 P-Q2` and `§7 #4` (the drift rule); `BUILD-CHECKLIST.md M7` reframed. Reversible — the Next app is host-agnostic, so a Vercel move stays cheap if global menu latency ever justifies it.

### 2026-08-05 · feature · M6 — AI menu import (photo/PDF → draft menu via Gemini), live-tested
- **area:** lib/ai, lib/mutations, apps/admin, infra
- **what:** Built the acquisition wedge: upload a photo/PDF of a paper menu → Gemini reads it into structured groups/sections/items/prices → editable review → **commit as a draft menu**. Parts: `lib/ai/gemini.ts` (Gemini REST, **no SDK**; `gemini-2.5-flash`; `responseSchema`-enforced JSON; zod-validated + cleaned; key ONLY from `process.env.GEMINI_API_KEY`; friendly error mapping for bad-key/rate-limit/safety-block/timeout). `lib/mutations/import-menu.ts` — `parseMenuUpload(formData)` (10MB cap, JPG/PNG/WEBP/HEIC/PDF) + `commitParsedMenu` (builds a **proper menu→groups→categories→items** hierarchy, decimal-safe `numeric(,2)` prices, unique slugs seeded from existing rows → routes into the new menu's editor). `components/admin/menu-import.tsx` (upload → fully editable tree → commit) fronting `/admin/import` (CSV demoted). `next.config.ts` server-action `bodySizeLimit: 12mb`.
- **notes:** **Live-tested end-to-end** on a rendered 11-item Italian menu (Chrome-headless screenshot): status 200, ~9s, **every item + price + section correct**, £ stripped + decimals preserved, 🌶→spice_level, (v)→vegetarian, and smart squid→seafood / guanciale→contains_pork. Key wired: **Railway** var set via `--stdin` (value never printed); **local** dev inherits it from the shell (`~/.zshrc`) so nothing is written to disk (honors the constraint). Commit path is code-verified (mirrors proven `createMenu`/CSV inserts + typechecks) — the UI click-through is auth-gated so Gold does the final in-app run. **Found a latent bug:** the legacy `importItemsCsv` creates orphan categories (no `group_id`) → invisible post-rescope; logged as a follow-up. Deferred: self-serve signup/onboarding shell + URL claim (M7), per-item confidence.

### 2026-08-05 · feature · M4 — manager dashboard (the `/admin` landing) + M4 core closed
- **area:** apps/admin
- **what:** Replaced the `/admin` → `/admin/menu` redirect with a real **dashboard**: venue name, three stat tiles (menus · total items · live/draft), **menus-at-a-glance** cards (each with Edit → `/admin/menu?m=`, Theme → `/admin/theme?m=`, View → `/menu?m=`), a New-menu button, a quick-actions row (add item · QR · import · settings), and a **View live site ↗**. Reuses `getAdminMenus()` (already returns venue name + per-menu counts). Login now lands on `/admin` (was `/admin/menu`); the "Platter" wordmark links home. **This closes M4's core** — with the editor rescoping below, the manager app is coherent per-menu.
- **notes:** Build + token gate green; `/admin` compiles. **Deferred as premature at single-venue:** the tenant/venue switcher + venues-list/detail — they belong to the Phase-2 SaaS surface and add nothing until a 2nd venue onboards (noted in `BUILD-CHECKLIST.md M4`). Real analytics (view counts, sold-out nudge, usage bar) on the dashboard are a later pass. **Natural checkpoint: M4 done → next milestone is M6 (onboarding + the Gemini PDF/photo import).**

### 2026-08-05 · feature · M4 — item editor rescoped per-menu (no more Dinner/Bar mixing)
- **area:** apps/admin, lib/queries
- **what:** The Editor (`/admin/menu`) now edits **one menu at a time** instead of every group in the venue. `getAdminMenuTree(menuSlug?)` scopes the tree by `group.menu_id` — the exact link the public `getMenu` uses (verified: Dinner owns 4 groups/10 items, Bar 1 group/4 items, **0 orphan categories**, so group→menu isolates cleanly). Added a **menu picker** (segmented control, hidden when ≤1 menu) + the active menu name as the header. Threaded the active-menu scope (`?m=<slug>`) through every path so it's sticky: **+ Item** → `getCategoryOptions(menuSlug)` offers only that menu's categories (a new item lands on the right menu); **+ Category** → `getGroupOptions(menuSlug)` offers only that menu's groups (auto-selects the sole starter group of a new menu) and returns to the editor; **Edit** links + item/category save/cancel all round-trip back to `/admin/menu?m=<slug>`. Edit-item keeps the full category list so an item can still be **moved across menus**. Closes the New-menu→add-items loop (a fresh menu's starter group can now receive its first category, then items).
- **notes:** Build + token gate green; all routes compile. Bulk "Move to…" now naturally scopes to the active menu (built from the scoped tree). Orphan categories (none today) surface only under the **default** menu as a fix-me bucket, never duplicated across menus. Still remaining in M4: app shell, dashboard, venues list — then M6 (Gemini import). Committing + redeploying.

### 2026-08-04 · feature+deploy · M4 menus-list + customiser deployed live
- **area:** apps/admin, lib/queries, lib/mutations, infra
- **what:** **Redeployed the M5 build to Railway** — `/admin/theme` customiser is now live in the deployed admin (deployment `b5c02106`, Online). Built M4's menu-management: `/admin/menus` (`lib/queries/admin-menus.ts` — venue's menus + per-menu item counts) with cards (theme · count · status) and a **New menu** flow (`lib/mutations/menus.ts createMenu` → draft menu + starter group → routes into the customiser). Nav gained "Menus"; the v1 item editor relabeled "Editor". **Model note (Gemini):** recorded that the AI features (M6 PDF/photo import, B5 concierge, optional B3) will use the user's **Gemini** key via a server `GEMINI_API_KEY` env var — never hardcoded or read from disk (`FEATURE-BACKLOG.md`).
- **notes:** Build + token gate green. The customiser is build-verified (admin is auth-gated — no screenshot without the owner login) but the publish→re-theme mechanism under it is proven live. **Biggest remaining M4 piece:** rescope the item editor (`/admin/menu`) to a *specific* menu — its queries/mutations are still restaurant-scoped, so with 2 menus it shows both mixed. Also remaining: app shell, dashboard, venues list. Committing + redeploying so `/admin/menus` is live too.

### 2026-08-04 · feature · M5 — theme customiser (core built)
- **area:** apps/admin, lib/themes, lib/mutations
- **what:** Built the customiser: `/admin/theme` (server page — loads the selected menu's theme + real items + the tenant plan; menu selector for Dinner/Bar List) + `components/admin/theme-customiser.tsx` (client split-screen: 4-theme picker with palette swatches, accent picker (curated + hex), scheme/layout toggles, and a **live phone preview of the venue's real items** that re-themes on every change via client `resolveTheme`). Publish/Discard via `lib/mutations/theme.ts` (`publishMenuTheme` / `saveMenuThemeDraft` → `theme_config_draft`), RLS-scoped (`requireManager` + `auth_can_manage`). `lib/themes/contrast.ts` = the WCAG **blocking 4.5:1** accent guard (shown as a live ratio; publish refused below it). Plan gating (Free = Lacquer only, rest Pro-locked). Added the Theme nav link.
- **notes:** Build + token gate green. **Visual test needs an owner login** (admin is auth-gated; can't screenshot without the password) — but the underlying **publish → re-theme mechanism is already proven live** (data-driven theme), so the customiser is the UI over a working path. Follow-ups: full "Reset to defaults", preview toggles for the item-sheet/search states, per-theme font loading. Uncommitted → committing.

### 2026-08-04 · deploy · ⭐ Validation gate LIVE on the hotel (Dinner=Lacquer ↔ Bar=Carafe)
- **area:** infra, db
- **what:** Redeployed the current build (M1–M4) to **Railway** — an interim deploy ahead of the locked P-Q2 Vercel cutover, so the *deployed* site runs the themed, menu-scoped build. Polled the live URL until the new build served (the `data-theme` marker appeared), then **published the 4 Bar drinks** (safe now — the live build scopes groups by menu, so bar items land only in the Bar List).
- **notes:** **Live-verified + screenshot-confirmed** on `platter-production-946c.up.railway.app`: `/menu` = Dinner in Lacquer (red, photos, seals, 0 bar-drink leakage); `/menu?m=bar-list` = Bar in **Carafe** (near-black + gold, small-caps heads, dotted-leader wine list, no images) with the switcher `[Dinner | Bar List]`. Tapping the switcher re-themes the whole page. **The "do themes sell?" gate is now tangible on the real hotel.** Committed through slice 2 (`54584f1`); this deploy + publish added no code (Railway deploy + a prod data update). Follow-up: import the full scraped bar list (M5/M6); the Vercel cutover remains M7.

### 2026-08-04 · feature · M4 (slice 2) — public menu switcher + Bar List; ⭐ validation-gate mechanism proven
- **area:** apps/public, components/menu, db
- **what:** Built `components/menu/menu-switcher.tsx` (segmented control over the venue's live menus; hidden when ≤1; `?m=` selects the menu → re-fetch → re-theme). Wired into the page (reads `?m=`, passes to `getMenu`, renders the switcher). Created on **prod**: renamed the default menu "Dinner", added a **Bar List** menu (live, `theme_id=carafe`) + a Bar group + Cocktails & Wine category + 4 **draft** drink items.
- **notes:** **Runtime-verified the validation-gate mechanism:** `/menu` = Dinner/Lacquer/red with switcher [Dinner | Bar List]; `/menu?m=bar-list` = Bar/**Carafe**/gold/ruled-list — navigating **re-themes the whole page**. Bar items are **draft on purpose** — the *deployed* old build (all-groups-by-restaurant query) would otherwise mix published Bar items into Dinner; confirmed **0 Bar items leaked** on the live site. **To finish the gate live:** deploy the new (menu-scoped) build, then publish the Bar drinks / import the full scraped bar list. That deploy is the P-Q2 Vercel cutover (M7) or an interim Railway redeploy — a decision point. Uncommitted → committing now.

### 2026-08-04 · feature · M4 (slice 1) — public menu is data-driven by the menus table
- **area:** lib/queries, apps/public, lib/supabase
- **what:** Made `getMenu(restaurantSlug, menuSlug?)` menu-aware — fetches the venue's live `menus` (with `theme_id`/`theme_config`), picks the active menu (requested slug → default → first), scopes groups + categories to it, and returns the theme + the menu list (`MenuSummary[]`, for a switcher). The public page now sources its theme from `menu.themeId`/`themeConfig` (M2's hardcoded `lacquer` removed); `?theme=` stays a preview override. **Regenerated `lib/supabase/database.types.ts`** from prod — the v1 types lacked `menus`/`memberships`/etc., which broke the build (supabase-js couldn't see the `menus` table).
- **notes:** Build green; runtime confirms `/menu` renders `data-theme=lacquer` **sourced from the DB default menu** (flip the record's `theme_id` → the menu re-themes, no code). For v1's single default menu the scoping is a no-op (it owns every group). **Remaining for the ⭐ validation gate:** create the hotel's **Bar List** menu (`theme_id=carafe`) + assign items, and a **public menu switcher** (venue home / segmented control) so guests toggle Dinner↔Bar. Uncommitted on branch `phase2/m1-tenancy`.

### 2026-08-04 · feature · M3b — Counter + Palm (all 4 launch themes shipped; theme system complete)
- **area:** lib/themes, components/menu
- **what:** Built **Counter** (`card-grid`, images-required, **LIGHT** scheme, bone-white + tangerine; `CardItem` photo grid) and **Palm** (`editorial` + `list-dense`, deep-green/raffia/ochre; `EditorialItem` full-bleed bands). Extended the layout abstraction so each layout owns its **list wrapper** (`layoutSpec()` → `{ Item, listClassName }`: divided list / photo grid / stacked bands); updated the page + `MenuBoard` to use it. All 4 registered in the registry.
- **notes:** **All 4 launch themes verified rendering the SAME menu data distinctly:** Lacquer (list-dense/dark/red/thumbs) · Carafe (ruled-list/dark/gold/dotted/no-images) · Counter (card-grid/**light**/tangerine/photo-grid) · Palm (editorial/dark/ochre/bands). 4 layouts · dark+light · images none/optional/required · 20 ₦ preserved throughout. Gate + build green. Built before the ⭐ validation gate (plan had them after) — defensible: themes are cheap (a manifest + one layout component each); the *expensive* work (customiser M5 / import M6 / billing M8) still waits on the gate. **The theme system (M2+M3) is COMPLETE.** Next: **M4** — surface a 2nd menu + switcher → the live validation gate (Dinner=Lacquer + Bar=Carafe on the hotel). Follow-ups: per-theme font loading (all reuse the 4 loaded faces now); item-sheet honoring `images:'none'`; Counter's `color-scheme:light`. Nothing committed — **M1→M3b now all uncommitted** on branch `phase2/m1-tenancy`.

### 2026-08-04 · feature · M3a — Carafe theme + layout abstraction (the abstraction PROOF passed)
- **area:** lib/themes, components/menu, apps/public
- **what:** Built **Carafe** (`lib/themes/carafe.ts`) — `ruled-list`, `images:'none'`, motif `none`, near-black + gold — and the **layout abstraction**: `RuledItem` (name → dotted-leader → price, no image/seal) as a SEPARATE component from `ItemRow`, a `LAYOUTS` map + `layoutItem()` (`components/menu/layouts.ts`), and layout-driven rendering in both the server grouped view (page) + the client `MenuBoard` filtered view (now takes a `layout` prop). Added a `?theme=` preview on the menu page (validated via `getTheme`; also powers M5). Fixed `item-row` `ring-offset-ink`→`ring-offset-bg` + tightened the token-gate regex to catch `-offset-` infixes.
- **notes:** **The PRD's abstraction test passed** — `images:'none'` renders with **no `{image && …}` hacks** (separate components, not conditionals in a shared row). Runtime-verified same-data-two-themes: `/menu` = list-dense/red/thumbs/0 dotted-leaders; `/menu?theme=carafe` = ruled-list/gold/near-black/**20 dotted-leaders + no images**; both keep 金餐厅 + 20 ₦. Lacquer unchanged (zero regression). Gate + build green. **`card-grid`/`editorial` (Counter/Palm) = M3b** — each is now just a manifest + one layout component. **The ⭐ validation gate (Dinner=Lacquer + Bar=Carafe live on the hotel) needs M4** (a 2nd menu surfaced + switcher). Follow-up: the shared item-sheet could honor `images:'none'`. Nothing committed (branch `phase2/m1-tenancy`).

### 2026-08-04 · feature · M2 core — theme system LIVE + Lacquer extracted (zero regression)
- **area:** lib/themes, apps/public, infra
- **what:** Built the theme system end-to-end: `components/theme/theme-provider.tsx` (SSR server component injecting `resolveTheme().cssVars` at the menu root — no FOUC). Completed the semantic token contract in `globals.css @theme` (`--color-bg`/`text`/`text-on-surface`/`text-secondary`/`on-accent` + existing), refactored all 7 menu components + the page **off the raw palette** onto the contract (`bg-ink`→`bg-bg`, `text-porcelain`→`text-text`, `text-muted`→`text-text-secondary`, seal glyph → `text-on-accent`), and wrapped the menu in `<ThemeProvider themeId="lacquer">`. Added the **⛔ no-raw-color gate** (`scripts/lint-tokens.sh` + `npm run lint:tokens`).
- **notes:** **Zero regression verified 3 ways:** structural (Lacquer's manifest = the v1 `@theme` defaults verbatim), `npm run build` green, and a **runtime render check** (built server → local `/menu` 200; SSR HTML carries `data-theme=lacquer` + `--color-accent:#8e1d1d`; 金餐厅 + 20 ₦ prices + real dishes). **M2 core done** — the system makes theming possible + Lacquer is live. **Deferred to M3/M4:** the other 3 themes (each = a new manifest), per-theme font loading (needs a 2nd theme to matter), the full 4-layout abstraction (only `list-dense` exists), and wiring `theme_id` from the real `menus` record (M4; hardcoded `lacquer` now). **Gate scope:** admin + OG route are documented non-themed exceptions. Nothing committed (branch `phase2/m1-tenancy`). **Next: M3a — Carafe** (proves `images:'none'`), then the ⭐ validation gate (Dinner=Lacquer + Bar=Carafe on the hotel).

### 2026-08-04 · feature · M2 started — theme foundation scaffolded; backlog features slotted into the phase plan
- **area:** lib/themes, context
- **what:** Slotted the 7 backlog features **into `BUILD-CHECKLIST.md`** (B6→M2, B2→M7, B3/B4/B7→M9, + B1/B5 as their own Phase-2 slots + a "Feature adds" map) so they're part of the phase plan, not orphaned. Started **M2**: mapped the v1 design system (`globals.css` `@theme`, `layout.tsx` fonts, `ui-tokens.md §2` semantic contract), then scaffolded the theme architecture — `lib/themes/types.ts` (the `ThemeManifest` contract), `lib/themes/lacquer.ts` (the v1 look extracted **verbatim**), `lib/themes/index.ts` (registry + `resolveTheme` → CSS-var map). **Typechecks clean.**
- **notes:** **Key M2 architecture:** the v1 menu already codes against semantic tokens (`ui-tokens §2`) with Lacquer's values as the `@theme` defaults — so the theme system = *complete the semantic contract + inject per-menu values via an SSR `ThemeProvider`*. Lacquer's values are identical to v1 → **zero regression** (the M2 ⛔ gate). **Remaining M2:** the SSR `ThemeProvider`; refactor `globals.css @theme` to the full semantic contract + components off raw utilities (`bg-ink`→`bg-bg`); per-theme `next/font`; wire `menus.theme_id`/`theme_config` into the public menu; the no-raw-color ESLint gate; screenshot-diff verification. (Also cleaned 24 `.next` "` 2`" folder-sync duplicates that were tripping `tsc`.)

### 2026-08-04 · feature · M1 tenancy seam LIVE on prod + verified; feature backlog added
- **area:** db, infra, context
- **what:** Applied **0006 (structural) + 0007 (RLS) + 0008 (hardening)** to **prod (Supabase)** via MCP. Backfill: 1 default **live** menu, all 4 groups re-parented (0 orphans), owner **membership** + subscription minted, 10 items untouched. RLS rewritten to membership-based `auth_tenant_ids()` — **63 policies, 4 new helpers, 14 anon**. Added **`docs/FEATURE-BACKLOG.md`** (7 candidate features: WhatsApp sold-out · print parity · no-result→one-tap-add · auto menu-engineering · guest LLM concierge · dual-currency · social kit) + pointer in `foundation.md §8`.
- **notes:** **M1's DB tenancy seam is LIVE and isolation-proven** (5/5 on the Railway clone; owner positive-control on prod). **Verified on prod:** live `/menu` still 200 with 金餐厅 + real dishes + 20 ₦ prices (anon path intact); owner impersonation → tenant + role `owner` + their 10 items/1 menu (admin login safe). Prod + Railway + repo migrations in lockstep (0001–0008). Remaining M1 (app-layer zod/query/mutation menu-scoping) **folds into M4** — v1 works as-is via RLS + the default menu. **Minor follow-ups:** vitest/CI wrapper for `tests/isolation.sql`; move `pg_trgm` out of `public`; enable Auth leaked-password protection (dashboard). Nothing committed (branch `phase2/m1-tenancy`). **Next: M2 — theme system + Lacquer extraction.**

### 2026-08-04 · decision+docs · Phase 2 kickoff — decisions locked, design spec + checklist, M1 branch started
- **area:** context, infra
- **what:** **Locked P-Q1** (validation-first: M1 → Lacquer → Carafe → prove on the hotel's Dinner + Bar menus → then Counter/Palm/customiser/import/billing) and **P-Q2** (migrate to **Vercel** before M7; v1 stays on Railway until the cutover; Vercel acct is on **Hobby** — Pro needed at M7 for domain automation). Updated `foundation.md §13` P-Q1/P-Q2 + a forward note on §7 #4. Wrote **`docs/DESIGN-SPEC.md`** (full product — 4 surfaces, 4 themes, every screen + state — for offline design) and **`docs/BUILD-CHECKLIST.md`** (every task in validation-first order). Created git branch **`phase2/m1-tenancy`**.
- **notes:** DB path resolved: Supabase branching needs **Pro** (project is Free) → pivoted to a **free local Postgres 17** (Homebrew, no Docker/cost) as the C2 test bed — cluster `.local-pg/` on :54329 (`pg_ctl` start needs `LC_ALL` set on macOS), `tests/fixtures/00_local_auth_stub.sql` emulates Supabase `auth`+roles; v1 migrations 0001–0004 apply clean = verified baseline. **Dev DB then moved to Railway** (Gold's call): Railway Postgres service `Postgres`, reached headlessly via `railway connect` (SSH tunnel + a registered SSH key; a duplicate `Postgres-5bml` service was cleaned up); the same stack was applied + verified there (25 tables, PG 18.4). Local pg kept (stopped) as fallback. **Authored + verified `0006_phase2_tenancy.sql`** (25 tables): `memberships`, `menus`, `menu_schedules`, `menu_id` on groups, tenant `plan_status`/billing, venue identity fields, platform tables + idempotent backfill (incl. owner membership, `§13 C3`). **Decisions this pass:** (a) **defer** the `restaurants`→`venues` rename (cosmetic, ripples through all code, zero isolation benefit — `restaurants` stays the venue table); (b) **§13 C5 already satisfied** — v1 `modifier_groups` already carry `restaurant_id`. **`0007_phase2_rls.sql` applied + isolation GREEN** on Railway: membership-based `auth_tenant_ids()` (array) + `auth_role_in`/`auth_can_manage`/`auth_is_admin`, `admin` role, all policies rewritten (63), staff column-guard triggers updated, guest reads NOT plan-gated (`§13 C1`). `tests/isolation.sql` — 5 tests (cross-tenant read, cross-tenant write, staff column-lock, anon published-only, manager positive control) — all pass. Manager `venue_ids` scoping deferred to the app layer (tenant isolation is the DB-enforced P0). **Remaining M1:** apply 0006+0007 to **prod (Supabase)** — needs go-ahead (live DB + deployed app); app-layer zod/query/mutation updates for the menu layer (mostly M4; v1 keeps working via the backfilled default menu + RLS). Vitest/CI wrapper for the isolation SQL is a small follow-up.

### 2026-08-04 · docs · Phase 2 platform PRD folded into the context system
- **area:** context
- **what:** Preserved the Phase 2 PRD verbatim as [`docs/PRD-platter-platform-phase2.md`](../docs/PRD-platter-platform-phase2.md) (the platform: multi-tenant venues/menus + the theme system), same treatment as the v1 PRD. Added **`foundation.md §13`** as the reconciled truth: the reframe, the target domain model (`restaurant`→`venue` + a new `menu` layer + theme registry), 8 Phase-2 decisions (P1–P8), **7 corrections that override the PRD (C1–C7)**, the Phase-2 deepest-risk note, and 4 open questions (P-Q1–P-Q4). Reconciled the **phase renumbering** in `foundation.md §8` (Phase 2 = Platform, Phase 3 = Ordering — flips v0). Extended `build-graph.md` with a Phase 2 dependency map (M1 keystone = isolation suite green; edges M2–M10; the cheapest-validation path). Pointers added to `CLAUDE.md` and the foundation header.
- **notes:** Nothing built — Phase 2 is **spec'd, not started**; v1 stays the deployed reality. Corrections worth flagging: **C1** guest reads must never gate on `plan_status` (PRD §5.2 contradicts §10 — a failed card would 404 a live menu); **C2** run the re-parenting migration on a Supabase branch first, drop the `restaurants`-as-a-view shim; **C3** backfill must mint the owner membership or admin locks out; **C4/C5** `menu.layout` single-source in `theme_config`, `modifier_groups` need `venue_id`. **Two open questions are blocking and need Gold:** P-Q1 (validation-first — ship Lacquer + Carafe on the hotel's Dinner/Bar menus as live proof — vs. committed vision build) and P-Q2 (hosting vs domains: Vercel or Cloudflare-for-SaaS in front of Railway, decide *before* M1, not at M7). No code, no schema changes; typecheck/build untouched.

### 2026-08-04 · chore · Deploy to Railway — live + verified
- **area:** infra
- **what:** Deployed to Railway (project `platter`, Nixpacks, `next start` on `$PORT`). **Live: https://platter-production-946c.up.railway.app**. Set `NEXT_PUBLIC_*` + `RESTAURANT_WHATSAPP` build-time vars; `NEXT_PUBLIC_SITE_URL` = the real domain so OG / sitemap / QR / canonical resolve. Pinned **Node 22** three ways (`engines.node`, `.nvmrc`, Railway `NIXPACKS_NODE_VERSION=22`).
- **notes:** First two builds **failed** — Nixpacks defaults to **Node 18**, which Next 16 (needs ≥20.9) rejects; the Node-22 pin fixed it → service **Online**. Verified live: `/menu` 200 renders 金餐厅 + real dishes + ₦ prices (Supabase reads work from Railway's IP), `/admin/login` 200, `/sitemap.xml` + `/api/og` 200 on the real domain, `/` 307→`/menu`. **Follow-ups:** (1) Supabase → Auth → URL config still localhost — add the Railway domain as Site URL + `…/auth/callback` for magic-link (email+password login already works). (2) Uncommitted: `package.json`, `.nvmrc`, `docs/RAILWAY-DEPLOY.md`, `scripts/legacy-menu.csv`.

### 2026-08-04 · chore · Legacy Instalacarte menu scraped → CSV (M5 data ready)
- **area:** infra, lib
- **what:** Scraped the live legacy menu → `scripts/legacy-menu.csv` — **19 categories, 258 items, 100% coverage** (extracted from the SPA's server-rendered pageContext JSON; DB untouched). Prices → plain NGN integers; names Title-Cased; typos fixed (Cripsy→Crispy, SHAWAMA→Shawarma, DRNKS→Drinks); portion counts pulled out of 2 names; every row `draft`, matches the 10-column importer header exactly (round-trip validated).
- **notes:** Ready for the deferred **M5** import via `/admin/import` (dry-run → publish). **Review before importing (source-data quirks, faithfully preserved — not extraction bugs):** 62 beverage rows have placeholder prices (58×₦1, 4×₦10 — hotel never set them); the 7 Drinks sub-categories are flattened into one "Drinks" (mapping preserved if we want to split); a few true duplicates in source (Ice Cream ×3, Calemari ×2, Geogold Special Sauce ×2) + 1 live-inactive item kept as draft.

### 2026-08-04 · feature · M6 — public search + filters, QR generator
- **area:** apps/public, apps/admin, lib
- **what:** **Search (P5) + filters (P6):** `MenuBoard` now owns a sticky header (search input + Vegetarian / Contains pork / Seafood / Spicy / Chef's-picks chips + the rail); when a search/filter is active it renders a client-filtered flat results list (the same `<a>` rows, so the sheet delegation still opens them) with a result count, Clear, and an empty state. Rail de-stickied (parent handles it); section scroll-offset widened. **QR generator (A14):** `/admin/qr` — branded QR preview, per-table `?t=` variant, download **SVG / PNG** + an **A6 table-tent PDF** (`@react-pdf`, code-split via `lib/table-tent`). New nav: QR.
- **notes:** Search + filters **verified live in-browser** (public, no login) — "soup" → 3 results; Chef's-picks → the 2 featured items; photos render; rail hides; Clear works. QR page builds; Gold tests the downloads. Substring match (not fuzzy) + local filter state (URL-state is a refinement). typecheck + build green. **Public menu is now feature-complete** (P1–P11, P14–P15 + search/filters).

### 2026-08-04 · feature · M4 — CSV import/export + modifier groups
- **area:** apps/admin, lib
- **what:** **CSV export** (`/api/export` — all items → CSV) + **import** (`/admin/import`: file or paste → client **dry-run preview** with per-row errors → `importItemsCsv` creates draft items, auto-creating categories by name). `lib/csv.ts` parser/serializer. **Modifier groups (A13):** full CRUD (`/admin/modifiers`, list/new/[id]) with an options editor; `saveModifierGroup`/`deleteModifierGroup` (cascade); **item assignment** via checkboxes in the item form (`setItemModifierGroups`). New nav: Add-ons, Data.
- **notes:** Verified via owner-session RLS SQL — modifier group + option insert and cascade-delete all work for owner. typecheck + build green (routes `/admin/import`, `/admin/modifiers[/new,/[id]]`, `/api/export`). Modifiers are *managed* now; public rendering of add-ons is Phase-2 (ordering). CSV import is the bridge to the deferred real-menu migration.

### 2026-08-04 · feature · M7 prep — SEO, OG images, sitemap
- **area:** apps/public
- **what:** Dynamic `generateMetadata` (menu + per-item deep-link title/description/canonical/OG/Twitter); **JSON-LD** (Restaurant + Menu + MenuItem with `offers`/`priceCurrency`); **`/sitemap.xml`** (menu + categories + items); **per-item OG image** at `/api/og` (`next/og`, brand frame, Latin-only for font safety) wired into `openGraph.images`; `metadataBase` in the root layout. `getMenu` wrapped in React `cache()` to dedupe the metadata + page + OG fetch.
- **notes:** Verified at runtime — OG image `200 image/png` (43KB), `sitemap.xml` `200` with correct URLs, JSON-LD present on `/menu`. OG co-location under the optional catch-all is disallowed by Next, so it's a plain `/api/og` route. Build green.

### 2026-08-04 · feature · M4 — bulk actions
- **area:** apps/admin, lib
- **what:** Multi-select in the menu tree (a checkbox per row via a React selection context) + a sticky **BulkBar**: mark sold out / available, publish / unpublish, **move to category**, **adjust price** (% or flat, rounded), delete, clear. Bulk mutations (`bulkSetAvailable`, `bulkSetStatus`, `bulkMoveCategory`, `bulkAdjustPrice`, `bulkDelete`) — manager+, tenant-scoped via `.in(ids)`.
- **notes:** typecheck + production build green. Selection clears after each action; price adjust affects `base_price` (variants are separate). Gold verifies interactively. **M4 so far:** variants, reorder, duplicate, bulk actions. Remaining M4: draft/publish batch + diff, CSV import/export, modifier groups.

### 2026-08-04 · feature · M4 (start) — drag-reorder + duplicate
- **area:** apps/admin, lib
- **what:** **dnd-kit** drag-to-reorder items within a category (a drag handle keeps the row's controls clickable; optimistic local order re-synced from the server; **fractional-midpoint** `sort_order` persisted via `reorderItem`, manager+; pointer + keyboard sensors). **Duplicate** button per row (`duplicateItem`, A10). `AdminItem` now carries `sort_order`; the tree component was rewritten (SortableItemRow / PlainItemRow / shared ItemBody).
- **notes:** typecheck + production build green (dnd-kit integrated cleanly). `reorderItem` is manager+ (the staff trigger also blocks any `sort_order` change). Reorder is within-category for now; category/group reorder + the rest of M4 (draft/publish batch + diff, bulk actions, CSV import/export, modifier groups) remain. Couldn't browser-test the drag myself (Gold's session is his own Chrome) — standard dnd-kit pattern, Gold verifies interactively.

### 2026-08-04 · feature · M3 — item variants + Settings page
- **area:** apps/admin, lib
- **what:** **Variants** (D7 completion): `getItemForEdit` now returns variants; `setItemVariants` mutation (replace-all, manager+); a variant editor in `ItemForm` (add/remove label + price rows). **Settings**: `getRestaurantSettings` + `saveSettings` (owner-only — restaurant name/中文/phone/whatsapp/address/currency/timezone/sold-out-reset/theme accent + 7-day opening hours); `SettingsForm` + `/admin/settings` page (nav link no longer 404s). Added `requireOwner` to `lib/rbac`.
- **notes:** Verified via owner-session RLS SQL — settings update (restaurants), variant insert **and** delete all allowed for owner. typecheck green. Skipped a production build this pass to avoid disrupting Gold's live dev session (Chrome, logged in); dev hot-reloads the new route/components. Gold confirmed the manager tree works in his browser. Remaining: staff accounts, audit log, analytics; the **M4** milestone (dnd reorder, draft/publish batch + diff, bulk actions, CSV import/export, modifier groups).

### 2026-08-04 · feature · M3 — category management (CRUD) — completes M3's defined scope
- **area:** apps/admin, lib
- **what:** Category mutations (`createCategory`, `updateCategory`, `setCategoryActive`, `softDeleteCategory` — blocked while it still has items, A12); queries (`getAdminCategories` with item counts + group name, `getGroupOptions`, `getCategoryForEdit`); `CategoryForm` (name, name_zh, description, slug, group picker, daypart from/to, visible toggle) + `CategoryList` (hide/show, edit, delete) at `/admin/categories`, `/new`, `/[id]`. Extracted `requireManager()` into `lib/rbac` (shared by item + category mutations).
- **notes:** Verified via owner-session RLS SQL — create → update → delete chain works; the non-empty guard has real data (appetizers = 4 items → delete blocked). Build green (all category routes). **M3's PRD-defined scope is now complete:** auth, RBAC (RLS + app layer), menu tree, item create/edit/soft-delete, category CRUD, sold-out toggle (optimistic), image upload (crop→WebP). Deferred admin extras (staff/settings/audit/analytics) + the **M4** milestone (dnd reorder, draft/publish batch + diff, bulk actions, CSV import/export, modifier groups, variants UI) remain.

### 2026-08-04 · feature · M3 — photo upload (Storage + crop → WebP)
- **area:** apps/admin, db, infra
- **what:** Storage bucket `menu-images` (migration `0005`): public-read, **tenant-scoped writes** for manager/owner via 4 RLS policies (path `{tenant_id}/{uuid}.webp`). Client pipeline: camera/gallery pick → **react-easy-crop** square crop → canvas → **WebP ≤200KB** (`lib/image.ts`) → upload via the browser Supabase client → public URL. `ImageUpload` (Radix crop dialog) wired into `ItemForm`; `image_url` added to `itemInsertSchema` + create/update mutations; `next.config.ts` `remotePatterns` for the Storage host.
- **notes:** Production build green (routes incl. `/admin/items/new` + `/admin/items/[id]`); bucket + **4 storage policies verified in SQL**. End-to-end upload needs a browser session — Gold tests it. Remaining in M3: category CRUD, staff/settings/audit/analytics.

### 2026-08-04 · feature · M3 Phase B — menu tree + sold-out toggle + item CRUD (verified via RLS)
- **area:** apps/admin, lib
- **what:** The core manager. Itemized:
  - Queries: `getAdminMenuTree` (RLS-scoped, drafts included), `getCategoryOptions`, `getItemForEdit`; `slugify` util.
  - Item mutations (server actions — role-checked, tenant-scoped, revalidating): `toggleItemAvailability`, `setItemStatus` (publish/unpublish), `softDeleteItem`/`restoreItem`, `createItem`, `updateItem`, `duplicateItem`.
  - UI: `MenuTree` (collapsible groups→categories via `<details>`, live filter, item counts, per-item Live/Draft toggle + sold-out switch + Edit + Del/undo); `SoldOutToggle` (optimistic + undo toast); `ItemForm` (react-hook-form — category, name, name_zh, description, price, compare-at, spice, prep time, dietary checkboxes, allergens, featured, available) at `/admin/items/new` and `/admin/items/[id]`.
- **notes:** Verified end-to-end via **owner-session RLS in SQL** (`set role authenticated` + jwt claims): read (owner sees 10 items / 3 categories), update (toggle + status, reverted), insert (draft item created), delete (owner delete allowed) — test row cleaned up. typecheck + production build green (Proxy active; routes `/admin`, `/admin/login`, `/admin/menu`, `/admin/items/new`, `/admin/items/[id]`). New items land as **draft**; the per-item publish toggle is the M3 precursor to M4's batch publish. **Remaining in M3:** category CRUD, **photo upload** (Storage + crop — item form has no image field yet), plus staff/settings/audit/analytics. Did not log in via the browser (verified through the API + SQL); Gold tests the UI.

### 2026-08-04 · feature · M3 Phase A — auth + RBAC foundation (verified)
- **area:** apps/admin, infra, db
- **what:** Supabase Auth login (password + magic link), **Next 16 `proxy.ts`** (middleware was renamed to Proxy in 16) that refreshes the session + does an optimistic `/admin` gate, RBAC (`lib/rbac` `getCurrentStaff` reading the staff row via RLS), the auth-gated admin workspace shell (nav + sign-out) at `app/(admin)/admin/(workspace)/`, a public `/admin/login`, and the `/auth/callback` route for magic links. Bootstrapped the owner: **gold.nwobu@gmail.com → owner of Jīn Cāntīng** (created via the signup API; confirmed + staff row inserted via MCP).
- **notes:** Verified — `/admin/menu` unauthed → 307 `/admin/login`; login page renders; owner credentials authenticate (token issued via the API). **Supabase confirmed to be in Gold's PERSONAL account** (org "goldhac's Org", project ref `bnyadozvvyzlzwnelrfu`) — not the company. Did **not** type the password into a form (verified auth via the token API instead). Next → Phase B: menu tree, item/category CRUD, the one-tap sold-out toggle, photo upload. Service-role key still only needed at M5.

### 2026-08-04 · feature · M2 public menu (read-only) — working & verified live
- **area:** apps/public, lib, db
- **what:** Built the real public menu at `/menu/[[...slug]]`. Itemized:
  - Data layer `getMenu()` assembles groups → categories → items (+variants) with a slug→item map; sold-out sinks to category bottom (P8); open/closed computed in the restaurant timezone.
  - Components: `MenuHeader` (bilingual mark + open/closed pill), `CategoryRail` (sticky, IntersectionObserver scrollspy, keyboard-operable), `ItemRow` (server `<a>`, seals, ledger price, thumb or seal-fallback), `ItemThumb`, `SealMark`, `ItemSheet` (Radix Dialog bottom sheet — focus-trap / Esc / aria-modal for free), `MenuBoard` (delegated click → `history.pushState` shallow routing + popstate sync). Root `/` → redirect to `/menu`.
  - Currency util unit-tested — 7 Vitest cases incl. the D1/D3 ("never a $") regression guard.
  - Real dish photos: generated 5 via the image tool (nano-banana), JPEG-optimized (~200KB) into `public/images`, wired to 5 items; the rest use the seal fallback (realistic mix, Assumption 3).
- **notes:** Verified live in-browser — thumbnails, sticky rail + scrollspy, bottom sheet with hero, and a **shareable shallow-routed URL** (`/menu/appetizers/hot-chicken-wings`, `role="dialog"`) all confirmed. typecheck + 7 tests green. Per Gold's steer this milestone prioritized **functionality + a11y + real examples**, not final visual polish (design overhaul is a later pass). Deferred: search/filters (M6); View-Transitions shared-element sheet + full axe/Lighthouse gate (M7); the manager photo-upload pipeline replaces the local `/images` seeds (M3/M5). Chow-mein image hit the image plan's daily grace limit → stays a fallback.

### 2026-08-04 · feature · M1 Foundation shipped (runs)
- **area:** infra, db, lib, apps/public
- **what:** Scaffolded the app and stood up the data layer end-to-end. Itemized:
  - Next.js 16.3 (App Router, TS strict, Turbopack) + Tailwind v4 + the design-token block; four fonts wired via `next/font` (Fraunces, Noto Serif SC [latin subset], Inter, IBM Plex Mono).
  - Supabase project **`platter`** created (ref `bnyadozvvyzlzwnelrfu`, region eu-west-2, $0/mo). Migrations `0001_init_schema` (PRD §8 + tenant seam, indexes), `0002_rls_policies` (RLS on all 16 tables, anon/staff/manager/owner, staff-availability-only trigger guard), `0003_advisor_hardening` applied.
  - `lib/schemas` (zod, the contract): restaurant, menu-group, category, item, variant, modifier + common. `lib/format/currency.ts` (the D1/D3 ₦ fix). `lib/supabase/{server,browser}.ts` typed with generated `database.types.ts`. `lib/queries/menu.ts`.
  - Seed: 1 tenant (De Geogold Hotel) → 1 restaurant (Jīn Cāntīng) → 3 categories → 10 published items (4 variants).
  - Proof page (`app/page.tsx`, force-dynamic): typecheck ✓, `next build` ✓, and a live server render showed all 10 items with correct `₦6,000` / `from ₦8,000` prices via anon RLS.
- **notes:** M1 done — **stopping before M2 per the PRD contract.** Service-role key is still blank in `.env.local` (not needed for M1; Gold pastes it from the dashboard before any admin work). Leftover create-next-app SVGs/favicon in `public/`+`app/` will be replaced in M2. Supabase security advisor: no ERRORs; residual WARNs are accepted/documented in `0003`. `.env.local` is git-ignored.

### 2026-08-04 · decision · Adopted Next.js 16 (PRD said 15)
- **area:** context
- **what:** `create-next-app@latest` installs Next 16.3 / React 19.2. Since this is greenfield and 16.3 is stable, adopted latest-stable rather than pinning back to 15.
- **notes:** Rippled into `foundation.md §7 #1`, `architecture.md` (diagram + stack table), `build-graph.md` L0.1, `library-docs.md`. App Router / RSC / server actions / `next/font` / `next/og` / `revalidateTag` are unchanged, so no design impact. Reversible (pin `next@15`) if a compat issue surfaces.

### 2026-08-04 · decision · M1 gating decisions locked
- **area:** context
- **what:** Gold confirmed the three M1 gates: (1) **tenancy** — lock in the multi-tenant schema / single-tenant surface (the `tenant_id` seam stays); (2) **Supabase** — create a **fresh** project; (3) **M1 seed** — **reconstruct from the PRD** now, scrape the legacy menu later at M5.
- **notes:** Updated `foundation.md §12` rows #2/#4/#5 to ✅ Locked. Cleared to begin M1. Still open (non-blocking): platform brand name (#1), exact "tenant" label (#3 — defaulting to *De Geogold Hotel* as the tenant with Jīn Cāntīng as its first restaurant for the seed), domain (#6), SaaS pricing (#7).

### 2026-08-04 · docs · Context system bootstrapped
- **area:** context
- **what:** Created the full source-of-truth context system for Platter: `foundation.md`, `project-overview.md`, `architecture.md`, `security.md`, `code-standards.md`, `library-docs.md`, `build-graph.md`, this log, the UI trio (`ui-tokens.md`, `ui-rules.md`, `ui-registry.md`), root `README.md` + `CLAUDE.md`. Preserved the origin PRD at `docs/PRD-jin-canting-menu.md`. Repo scaffolded and `git init`'d on `main`.
- **notes:** No application code yet — M1 has not started. Key decisions this session: (1) reframed the single-restaurant PRD into a **multi-tenant platform** (codename `Platter`) shipping one tenant (Jīn Cāntīng) in v1; (2) chose **row-level multi-tenancy** — a `tenants` seam + denormalized `tenant_id` + RLS — as schema-only in v1 (`foundation.md §7 #11/#12`); (3) split **tenant isolation** into its own authority file `security.md`; (4) drafted the UI trio directly from PRD §10 rather than from a Claude Design export, since the design system is already fully specified. Open items for Gold to confirm before/at M1: `foundation.md §12` (platform name, tenancy-model sign-off, who the "tenant" is, new-vs-existing Supabase project, M1 seed-data source, domain).

### 2026-08-04 · decision · Multi-tenant from the schema, single-tenant in the product
- **area:** context
- **what:** Overrode the PRD's "multi-tenant SaaS = non-goal." v1 still ships one venue, but the data model carries the tenant seam so the SaaS surface is additive later, not a re-migration.
- **notes:** Recorded in `foundation.md §7 #11/#12`, `§8` (SaaS deferred), `§9`, `§10`; mechanism in `architecture.md → Data & tenancy`; isolation policy in `security.md`. This is the one architectural call made *for* Gold — flagged for sign-off in `foundation.md §12 #2`.

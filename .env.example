# Platter — env template. Copy to .env.local and fill in. See context/security.md §4.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — bypasses RLS, never exposed to the client (security.md §3).
SUPABASE_SERVICE_ROLE_KEY=

NEXT_PUBLIC_SITE_URL=http://localhost:3000
RESTAURANT_WHATSAPP=
